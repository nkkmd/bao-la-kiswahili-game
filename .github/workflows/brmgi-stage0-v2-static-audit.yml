name: BRMGI Stage 0 v2 Static Audit
on:
  push:
    branches:
      - research/g3-06-bao-rule-mechanism-geometry-intervention
    paths:
      - doc/bao-rule-mechanism-geometry-intervention/checkpoints/brmgi-stage0-v2-static-audit-trigger.txt
concurrency:
  group: brmgi-stage0-v2-static-audit
  cancel-in-progress: false
permissions:
  contents: read
jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Static audit v2 only
        run: node tools/experiments/audit-brmgi-stage0-v2-static.js | tee BRMGI_STAGE0_V2_STATIC_AUDIT.json
      - name: Upload v2 static audit artifact
        uses: actions/upload-artifact@v4
        with:
          name: brmgi-stage0-v2-static-audit-${{ github.run_id }}
          path: BRMGI_STAGE0_V2_STATIC_AUDIT.json
          if-no-files-found: error
