name: GTTD G4-04 Stage 2 formal once — CLOSED

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  closed-no-rerun:
    runs-on: ubuntu-latest
    steps:
      - name: Refuse same-evidence rerun
        run: |
          echo "GTTD-STUDY1 Stage 2 completed canonically in run 36095831961."
          echo "Same-evidence rerun, seed extension, and post-access repair-and-rerun are not authorized."
          exit 1
