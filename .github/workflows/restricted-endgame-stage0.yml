name: Restricted endgame Stage 0 technical validation

on:
  push:
    branches:
      - research/restricted-endgame-winning-regions
    paths:
      - "public/engine.js"
      - "tools/benchmark.js"
      - "tools/experiments/lib/restricted-endgame-retrograde.js"
      - "tools/experiments/lib/restricted-endgame-transition.js"
      - "tools/experiments/lib/restricted-endgame-stage0.js"
      - "tools/experiments/run-restricted-endgame-stage0-feasibility.js"
      - "tools/experiments/run-restricted-endgame-stage0-v2-benchmark.js"
      - "test/restricted-endgame-retrograde.test.js"
      - "test/restricted-endgame-transition.test.js"
      - "test/restricted-endgame-stage0.test.js"
      - "doc/restricted-endgame-winning-regions/preregistration/STAGE_0_TECHNICAL_SPEC.json"
      - "doc/restricted-endgame-winning-regions/preregistration/STAGE_0_TECHNICAL_SPEC_V2.json"
      - ".github/workflows/restricted-endgame-stage0.yml"
  pull_request:
    paths:
      - "public/engine.js"
      - "tools/benchmark.js"
      - "tools/experiments/lib/restricted-endgame-retrograde.js"
      - "tools/experiments/lib/restricted-endgame-transition.js"
      - "tools/experiments/lib/restricted-endgame-stage0.js"
      - "tools/experiments/run-restricted-endgame-stage0-feasibility.js"
      - "tools/experiments/run-restricted-endgame-stage0-v2-benchmark.js"
      - "test/restricted-endgame-retrograde.test.js"
      - "test/restricted-endgame-transition.test.js"
      - "test/restricted-endgame-stage0.test.js"
      - "doc/restricted-endgame-winning-regions/preregistration/STAGE_0_TECHNICAL_SPEC.json"
      - "doc/restricted-endgame-winning-regions/preregistration/STAGE_0_TECHNICAL_SPEC_V2.json"
      - ".github/workflows/restricted-endgame-stage0.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  technical-validation:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Validate baseline engine
        run: node test/engine.test.js
      - name: Validate synthetic retrograde semantics
        run: node test/restricted-endgame-retrograde.test.js
      - name: Validate guard-free Mtaji transition parity
        run: node test/restricted-endgame-transition.test.js
      - name: Validate witness and closure mechanics
        run: node test/restricted-endgame-stage0.test.js
      - name: Run v1 technical-only witness/root scan
        run: |
          node tools/experiments/run-restricted-endgame-stage0-feasibility.js \
            --mode scan \
            --output /tmp/rewr-stage0-v1 > /tmp/rewr-stage0-v1-console.json
      - name: Assert v1 scientific outcome firewall
        run: |
          node - <<'NODE'
          const fs = require('node:fs');
          const p = '/tmp/rewr-stage0-v1/stage0-technical-manifest.json';
          const r = JSON.parse(fs.readFileSync(p, 'utf8'));
          if (r.technicalOnly !== true
              || r.scientificOutcomeGenerationAuthorized !== false
              || r.scientificTablebaseGenerationAuthorized !== false
              || r.outcomeFieldsEmitted !== false
              || r.benchmark !== null) {
            throw new Error('Stage 0 v1 firewall violation');
          }
          const forbidden = /\b(WIN|LOSS|RECURRENT|DTF|optimalMoveSet|absoluteWinner)\b/;
          if (forbidden.test(JSON.stringify(r.scan))) {
            throw new Error('Scientific outcome token leaked into v1 scan output');
          }
          console.log(JSON.stringify({
            stageId: r.stageId,
            specSha256: r.specSha256,
            uniqueWitnessRoots: r.scan.uniqueWitnessRoots,
            candidatePlanCount: r.candidatePlans.length,
            outcomeFieldsEmitted: r.outcomeFieldsEmitted,
            manifestSha256: r.manifestSha256,
          }, null, 2));
          NODE
      - name: Run v2 initial complete-closure benchmark
        run: |
          node tools/experiments/run-restricted-endgame-stage0-v2-benchmark.js \
            --output /tmp/rewr-stage0-v2-initial-closure.json \
            > /tmp/rewr-stage0-v2-console.json
          cat /tmp/rewr-stage0-v2-console.json
      - name: Assert v2 scientific outcome firewall
        run: |
          node - <<'NODE'
          const fs = require('node:fs');
          const r = JSON.parse(fs.readFileSync('/tmp/rewr-stage0-v2-initial-closure.json', 'utf8'));
          if (r.technicalOnly !== true
              || r.scientificOutcomeGenerationAuthorized !== false
              || r.scientificTablebaseGenerationAuthorized !== false
              || r.outcomeFieldsEmitted !== false) {
            throw new Error('Stage 0 v2 firewall violation');
          }
          const forbidden = /\b(WIN|LOSS|RECURRENT|DTF|optimalMoveSet|absoluteWinner)\b/;
          if (forbidden.test(JSON.stringify(r))) {
            throw new Error('Scientific outcome token leaked into v2 benchmark output');
          }
          NODE
      - uses: actions/upload-artifact@v4
        with:
          name: rewr-stage0-technical-audits
          path: |
            /tmp/rewr-stage0-v1/stage0-technical-manifest.json
            /tmp/rewr-stage0-v1/witness-roots.json
            /tmp/rewr-stage0-v2-initial-closure.json
          if-no-files-found: error
          retention-days: 14
