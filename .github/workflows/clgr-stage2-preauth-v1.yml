name: CLGR Stage 2 Preauthorization Audit v1

on:
  push:
    branches:
      - research/g3-09-continuous-local-geometry-representation
    paths:
      - doc/continuous-local-geometry-representation/authorizations/clgr-stage2-preauth-v1-trigger.txt

permissions:
  contents: read

concurrency:
  group: clgr-stage2-preauth-v1
  cancel-in-progress: false

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2
      - name: Syntax-check Stage 2 frozen tooling
        run: |
          node --check tools/experiments/run-clgr-stage2-preauth-audit.js
          node --check tools/experiments/run-clgr-stage2-formal.js
          node --check tools/experiments/verify-clgr-stage2-authorization.js
          node --check tools/experiments/lib/clgr-stage2-production.js
          node --check tools/experiments/lib/clgr-stage2-independent.js
      - name: Run fresh-free Stage 2 preauthorization audit
        run: |
          mkdir -p artifacts/local/continuous-local-geometry-representation/stage2-preauth-v1
          node tools/experiments/run-clgr-stage2-preauth-audit.js \
            --output artifacts/local/continuous-local-geometry-representation/stage2-preauth-v1/PREAUTH_AUDIT_RESULT.json
      - name: Upload audit result
        uses: actions/upload-artifact@v4
        with:
          name: clgr-stage2-preauth-v1-${{ github.run_id }}
          path: artifacts/local/continuous-local-geometry-representation/stage2-preauth-v1/PREAUTH_AUDIT_RESULT.json
          if-no-files-found: error
          overwrite: false
