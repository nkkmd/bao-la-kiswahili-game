name: EBRWS main integration status sync — CLOSED / DISABLED

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  closed:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "G3-02 main integration status synchronization is complete; this workflow is disabled."
