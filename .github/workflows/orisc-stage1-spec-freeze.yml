name: ORISC Stage 1 spec freeze validation — closed study

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  closed-study:
    runs-on: ubuntu-latest
    steps:
      - name: Closed-study notice
        run: |
          echo "ORISC-STUDY1 is completed."
          echo "Stage 1 spec refreeze/validation is no longer authorized within this study."
          echo "The final frozen spec and validation provenance are recorded in doc/oracle-representation-integrity-symmetry-confirmation/REPRODUCIBILITY_INDEX.md."
