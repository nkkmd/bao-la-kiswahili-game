name: EBRWS final document consistency fix — CLOSED / DISABLED

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  closed:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "G3-02 final document consistency synchronization is complete; this workflow is disabled."
