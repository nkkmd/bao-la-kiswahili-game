name: EBRWS Stage 0 technical — CLOSED / DISABLED

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  closed:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "EBRWS-STUDY1 is CLOSED / TECHNICAL-INVALID; Stage 0 technical validation is no longer active."
