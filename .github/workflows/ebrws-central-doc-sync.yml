name: EBRWS start central document sync — CLOSED / DISABLED

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  closed:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "EBRWS-STUDY1 is CLOSED; start-state central-document materialization is disabled."
