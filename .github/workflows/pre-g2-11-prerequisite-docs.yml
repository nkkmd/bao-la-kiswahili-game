name: Pre-G2-11 Prerequisite Documentation

on:
  push:
    branches:
      - maintenance/pre-g2-11-strategic-representation-prerequisite
    paths:
      - 'tools/experiments/materialize-pre-g2-11-prerequisite-docs.py'
      - '.github/workflows/pre-g2-11-prerequisite-docs.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  materialize:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout preparation branch
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Materialize central documentation
        run: |
          python3 tools/experiments/materialize-pre-g2-11-prerequisite-docs.py
          git diff --check

      - name: Enforce documentation-only allowlist
        run: |
          python3 - <<'PY'
          import subprocess
          allowed = {
              'doc/FUTURE_RESEARCH_AGENDA.md',
              'doc/RESEARCH_INDEX.md',
              'doc/research-program-decisions/2026-08-26-second-generation-pure-research-agenda.md',
          }
          changed = set(subprocess.check_output(['git','diff','--name-only'], text=True).splitlines())
          unexpected = sorted(changed - allowed)
          if unexpected:
              raise SystemExit('unexpected materializer paths: ' + ', '.join(unexpected))
          print('allowlist PASS')
          PY

      - name: Commit central documentation
        run: |
          if git diff --quiet; then
            echo 'No central documentation changes required.'
            exit 0
          fi
          git config user.name 'Sawa'
          git config user.email '72678394+nkkmd@users.noreply.github.com'
          git add doc/FUTURE_RESEARCH_AGENDA.md doc/RESEARCH_INDEX.md \
            doc/research-program-decisions/2026-08-26-second-generation-pure-research-agenda.md
          git commit -m 'docs(research): register pre-G2-11 prerequisite study direction'
          git push origin HEAD:maintenance/pre-g2-11-strategic-representation-prerequisite
