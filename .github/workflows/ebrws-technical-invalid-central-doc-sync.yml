name: EBRWS closure central document sync — CLOSED / DISABLED

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  closed:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "EBRWS-STUDY1 closure synchronization is complete; this workflow is disabled."
