# LGTTCI-STUDY1 — Stage 0 PASS

日付: 2026-09-17  
Stage: `LGTTCI-S0-TECHNICAL-2026-09-17-v1`  
正式判定: **`STAGE0-PASS`**

## 実行provenance

```text
Draft PR = #151
GitHub Actions run = 35187213672
Job = 105091747332
Head branch = research/g4-01-transfer-compatibility-instrument
Head SHA at execution = 8ef15ff4e72a781d3b6fdb065fdae012772a57a9
Artifact ID = 10482153364
Artifact name = lgttci-stage0-result
Artifact ZIP SHA256 = 3b5b2f64568b0a5817ede4ef44b5552a8ee531c22ce56722d2ca2fe8f84491c7
Result deterministic core SHA256 = 3579d3bc7f4c15495b5ba75b6c69edeba9caf84f24b7d541fa4b6fe14ca207e0
```

workflowは`contents: read`だけで実行され、syntax check、technical-only Stage 0、artifact uploadの全stepがsuccessだった。

## 主要な検証結果

全必須checkがPASSした。

- frozen source blob binding
- production / independent static boundary
- source-policy replay exact一致
- 新しい2つのsource policyがtechnical fixture上で実際に異なるtrajectoryを生成すること
- root contractの実行可能性
- singleton rootをhelper呼出し前に検出すること
- G3-12 failure modeを再現するnegative control
- RAW identity / canonical move identity一致
- relative depth 5 RAW geometry exact一致
- rankable rootでのcontrolled-search compatibility exact一致
- continuous geometry exact一致
- bounded preflight exact一致
- malformed RAW identityのfail-closed
- deterministic replay
- protected evidence firewall

## G3-12 failure lessonの直接検証

frozen technical fixtureから、次のreachable singleton rootを取得した。

```text
policy = LGTTCI-P1-UNIFORM-LEGAL
seed = 44010001
ply = 44
phase = mtaji
rootLegalWidth = 1
rawStateSha256 = 5aa1b939961884ef0c994d78d026f4437ce4508e3a368addc9c196fdc9274f1b
```

compatibility wrapperはこのrootを次のように処理した。

```text
estimable = false
reasonCode = ROOT-LEGAL-WIDTH-LT2
helperCalled = false
```

一方、同じrootをwrapperを迂回して既存helperへ直接渡すnegative controlでは、G3-12で問題になったpreconditionが再現された。

```text
production = complete root ranking required
independent = ranking requires >=2 moves
```

したがって、G4-01のcompatibility layerは、G3-12でStage 0が見逃したgapを科学的populationへ進む前に検出・遮断できる。

## source policyとroot familyのtechnical support

64 technical fixture seedsのうち、P1とP2でtrajectory digestが異なったseedは63件だった。

complete root family supportは次のとおり。

| Root family | P1 | P2 |
| --- | ---: | ---: |
| `LGTTCI-RF1-MID-ANCHOR` | 41 | 62 |
| `LGTTCI-RF2-OFFSET-ANCHOR` | 28 | 58 |

これはStage 1のsupport条件を保証する結果ではないが、root selectorが両policy・両familyで実行可能であることをtechnical fixture上で確認した。

## representative root検証

NamuaとMtajiからresource-eligibleなrankable rootを各1件選び、RAW geometry、continuous geometry、全6 search conditionについてproduction/independent exact一致を確認した。

Namua代表:

```text
seed = 44010001
ply = 3
rootLegalWidth = 2
distinctRawStates = 459
uniqueTransitions = 505
treeNodeOccurrences = 526
```

Mtaji代表:

```text
seed = 44010001
ply = 44
rootLegalWidth = 2
distinctRawStates = 271
uniqueTransitions = 270
treeNodeOccurrences = 271
```

## resource ceiling

```text
elapsedMs = 12482 <= 600000
peakRssBytes = 129564672 <= 1500000000
resultArtifactBytes = 9494 <= 8000000
```

すべてceiling内だった。

## protected evidence firewall

```text
fresh compatibility seed access = false
G3-12 Stage 1 seed replay = false
G3-12 Stage 2 seed access = false
G3-11 depth 10 rerun = false
G4-10 depth 11 access = false
formal effect computation = false
protected evidence access = false
```

## 帰結

Stage 0 PASSはupstream claimのgeneralization確認ではない。fresh compatibility populationを安全に生成・評価するためのtechnical readinessが成立したことだけを示す。

Stage 1はこの結果だけでは自動承認されない。次に**post-Stage0 Stage 1 authorization review**を行う。
