# PCRPR-STUDY1 — Study-start prospective freeze

Date: 2026-08-29
Status: FROZEN BEFORE ANY G2-07 SCIENTIFIC OUTCOME GENERATION
Program: G2-07 / Research Generation 2
Study ID: `PCRPR-STUDY1`
Formal English title: **Practical Comeback / Reply-Pressure Representation Study 1**
Japanese title: **Baoにおける実戦的逆転可能性とreply pressureの豊かな機械表現の構築・prospective検証 — reply-set width, defense-maintaining reply fraction, reply-quality distribution, punishment concentration, and opponent-policy sensitivity によるpractical comeback structureの再現可能な記述**
Baseline remote `main`: `e5ad840520eb6c5bd0408f924c1f9a55c4cba6a5`
Research branch: `research/g2-07-practical-comeback-reply-pressure-representation`

## 1. Program position and independence

`G2-07` is the next unstarted machine-only Research Generation 2 agenda item after closure of `G2-01..G2-06`.

This is a new prospective independent Study. It is not PCEM-STUDY1 Study 2, not a PCEM rescue, and not a continuation/repair/rescue of RCPR-STUDY1.

No completed formal decision, negative/null/inconclusive/non-estimable/not-authorized/technical-invalid result, threshold, classifier, endpoint, population, representation boundary, or interpretation boundary may be changed by PCRPR-STUDY1.

## 2. Immutable upstream state

Research Generation 2:

```text
G2-01 / PEOCR-STUDY1 = INCONCLUSIVE
G2-02 / SRDR-STUDY1 = INCONCLUSIVE
G2-03 / STSCV-STUDY1 = INCONCLUSIVE
validated transform set = []
canonicalization = NOT AUTHORIZED
G2-04 / REEOE-STUDY1 = INCONCLUSIVE
G2-04 Stage 2 = NOT-AUTHORIZED-NOT-EXECUTED
G2-05 / DRSSE-STUDY1 = EXACT-WITHIN-FROZEN-DEPTH-9-DOMAIN
G2-06 / RCPR-STUDY1 Stage 0 = STAGE0-TECHNICAL-PASS
G2-06 / RCPR-STUDY1 Stage 1 = STAGE1-TECHNICAL-INVALID
G2-06 Stage 1 seed block 28610001..28613072 = CONSUMED
G2-06 same-block rerun = NOT AUTHORIZED
G2-06 replacement / extension = NOT AUTHORIZED
G2-06 Stage 2 = NOT-AUTHORIZED-NOT-EXECUTED
```

Direct Research Generation 1 upstream `PCEM-STUDY1`:

```text
Stage 0 = TECHNICAL-PASS
Stage 1 = EXPLORATORY-ONLY / COMPLETE
source games = 3072
selected disadvantaged roots = 300
exact root-move interventions = 1065
total continuation rows = 18105
candidate grammar = PCEM-T1..T8
candidate audits = 55
promoted candidates = 0
Stage 2 = NOT-AUTHORIZED-NOT-EXECUTED
reserved Stage 2 seeds 23300001..23306144 = unconsumed
```

The `PCEM-T1..T8` grammar, 55 audits, thresholds, near misses, Stage 1 rows, and unconsumed reserved Stage 2 block are not PCRPR scientific evidence and are not eligible for rescue, extension, or promotion.

Research Generation 1 Critical Positions / Outcome Branching, Blunder / Misvaluation Patterns, Position Complexity, Position Evaluation, Tactical Motifs and all other completed decisions remain immutable.

Historical data may be used only as method-design reference or non-outcome technical fixture when the historical outcome/label is not consumed. It is not PCRPR development or formal evidence.

## 3. Human interpretation firewall

PCRPR-STUDY1 studies **machine-operational reply pressure / practical comeback structure** only.

```text
machine reply pressure
!= human difficulty
!= human deception
!= human error probability
!= psychological pressure
!= expert-perceived complexity
```

No human-difficulty, human-error, deception, confusion, psychology, or expert-recognition claim is authorized without a separate prospective human-participant study.

Opponent-policy sensitivity is sensitivity to the prospectively frozen machine policy family only. It does not generalize to people.

## 4. Authoritative scientific identity

Scientific state identity remains RAW-only and contains exactly:

```text
pits
reserve
houseOwned
player
phase
winner
pending
```

Excluded from RAW identity:

```text
turn
reason
```

Missing `pending` fails closed. Seed conservation is mandatory. The validated transform set is exactly `[]`.

```text
canonicalization = false
symmetry reduction = false
transform-based deduplication = false
```

No G2-07-specific symmetry or canonicalization is authorized.

## 5. Scientific unit and construct separation

The primary representation unit is a **historically observed RAW root occurrence × exact root-move variant**.

The Study must keep the following quantities separate:

```text
baseline reference position value
root-move-conditioned reference value
reply-conditioned reference value
machine opponent-policy-dependent continuation value
reply-pressure structural descriptors
```

A practical-comeback structure is not defined as merely the best move, highest evaluation move, highest bounded win-frequency move, or a move that works only because a weak policy was chosen.

## 6. Leakage classes

Every predictor feature belongs to exactly one class:

```text
A = PRE_ROOT_OBSERVABLE
B = ROOT_OR_REPLY_DERIVED_OUTCOME_INDEPENDENT
C = SEARCH_OR_POLICY_DISTRIBUTION_DERIVED_OUTCOME_INDEPENDENT
D = CONTINUATION_OR_FUTURE_OUTCOME_DERIVED
```

Only A-C are predictor-eligible. D is forbidden from the representation.

Class D includes at minimum terminal continuation result, future winner, empirical bounded comeback frequency, policy-conditioned terminal outcome, post-root sampled rollout state, formal target, and any quantity computed from those outcomes.

Reply graphs, exact reply sets, deterministic successor geometry, and search scores are eligible only when generated from the root/root-move/reply state under a completely frozen outcome-independent instrument.

## 7. Prospectively declared representation families

PCRPR Stage 0/1 may evaluate only the following candidate families:

1. `REPLY_SET_WIDTH`
2. `DEFENSE_MAINTAINING_REPLY_FRACTION`
3. `REPLY_QUALITY_DISTRIBUTION`
4. `PUNISHMENT_CONCENTRATION`
5. `BEST_REPLY_GAP_VECTOR`
6. `FORCING_REPLY_STRUCTURE`
7. `REPLY_BRANCH_ASYMMETRY`
8. `REPLY_SEARCH_STABILITY`
9. `OPPONENT_POLICY_SENSITIVITY`
10. `ROOT_MOVE_REFERENCE_CONTEXT`
11. `LOCAL_TACTICAL_GEOMETRY_RESERVE_HOUSE_PHASE`
12. `LOCAL_TEMPORAL_CONTEXT`

No new feature family may be introduced after Stage 1 scientific outcomes are observed.

`OPPONENT_POLICY_SENSITIVITY` as a predictor is restricted to outcome-independent reply-selection distributions or deterministic policy scores over the exact reply set. Empirical continuation outcomes under those policies are target/outcome data and cannot enter predictor features.

Stage 0 may exclude a declared family before Stage 1 scientific generation for leakage, ambiguous semantics, non-determinism, missingness, independent-recomputation failure, or resource infeasibility. Such exclusion is a technical disposition, not a scientific result.

## 8. Stage architecture and identities

```text
Stage 0 = PCRPR-S0-TECHNICAL-2026-08-29-v1
Stage 1 = PCRPR-S1-DEVELOPMENT-2026-08-29-v1
Stage 2 = PCRPR-S2-FORMAL-2026-08-29-v1
```

Stage 0 is technical / representation feasibility only and cannot produce a scientific conclusion.

Stage 1 is fresh development-only evidence. It may construct and freeze one machine reply-pressure representation/scoring contract under a preregistered development rule.

Stage 2 is fresh independent formal validation only. Stage 2 does not become authorized automatically if Stage 1 succeeds. A separate transition freeze, source/hash freeze, verifier freeze, fresh population freeze, and explicit authorization are mandatory.

If Stage 1 cannot form a formal target under its frozen procedure, Stage 2 remains `NOT-AUTHORIZED-NOT-EXECUTED`.

## 9. Stage 0 technical / numeric hardening contract

G2-06's floating-point mismatch is treated only as a predecessor technical lesson. PCRPR must validate its own numeric contract before any scientific seed is consumed.

Mandatory rules:

1. exact move/reply categories are ordered by one canonical exact-move-key rule before any aggregation;
2. aggregations over categories use a prospectively specified deterministic accumulation order;
3. integer-like object-key enumeration order is never accepted as an implicit scientific ordering rule;
4. count/fraction features retain canonical integer numerator/denominator source values where possible;
5. finite binary64 feature values are canonically serialized for hashing using an explicitly frozen IEEE-754 byte/hex contract rather than locale-dependent formatting;
6. production and independent implementations may share the authoritative Bao rule engine but must not import each other's feature, serializer, aggregation, model, or result-assembly helpers;
7. adversarial fixtures must include integer-like keys whose encounter order differs from numeric order, reply-order permutations, ties, zero/one/many replies, terminal successors, missing/corrupted `pending`, search-profile drift, feature-schema drift, future-outcome leakage, and RAW-key mismatch;
8. representation features intended to be exact must pass exact cross-implementation equality before Stage 1 authorization;
9. if any later statistical quantity requires non-exact numeric comparison, its comparison semantics and technical rationale must be frozen before the corresponding scientific outcome exists.

No tolerance or rounding rule may be introduced after scientific outcomes are observed to rescue PCRPR or RCPR.

## 10. Fresh population and seed reservation

The study-start baseline scientific population design is:

```text
Stage 1 source games = 3072
Stage 1 reserved seeds = 28710001..28713072
Stage 1 use = CONSUME-ONCE-DEVELOPMENT-ONLY
Stage 1 max observed ply = 100
opening prefix = 8 plies
opening generation = seeded uniform over lexically sorted exact move variants
Stage 1 root target = 400 (Namua 200 / Mtaji 200)

Stage 2 reserved seeds = 28810001..28816144
Stage 2 reserved game count = 6144
Stage 2 use = FORMAL-ONLY / UNCONSUMED AT STUDY START
```

The Stage 1 generation-stratum baseline uses the same six **outcome-independent generation mechanisms** previously used as a methodological population-diversity reference, but with fresh PCRPR seeds and no inherited evidence:

```text
B-D1  = bao / phase2 / depth1 / q1
B-D2  = bao / phase2 / depth2 / q1
B-D3  = bao / phase2 / depth3 / q1
LS-D2 = bao / legacy / depth2 / q0
V2-D2 = bao-v2 / phase2 / depth2 / q1
LE-D2 = legacy-evaluation / phase2 / depth2 / q1
```

Assignment baseline is deterministic game-index modulo 6. Exact generator/source hashes must still be frozen after Stage 0 and before Stage 1 authorization.

This population design may be superseded only by an explicit **pre-scientific-outcome** amendment justified by Stage 0 technical/resource evidence, with zero Stage 1 scientific seeds consumed. Post-consumption replacement or extension is not authorized.

## 11. Trajectory, RAW duplication, and root-selection baseline

Trajectory identity is the hash of the ordered RAW state-key sequence. Opening-prefix identity hashes the explicit first 8 exact move keys.

Duplicate historical trajectories are collapsed by deterministic minimum seed then game index.

Root selection is outcome-blind with respect to PCRPR continuation targets:

1. assign each unique trajectory to Namua or Mtaji by a frozen salted hash;
2. within the assigned phase, choose exactly one candidate occurrence by minimum salted hash rank before continuation-target inspection;
3. require nonterminal state and at least two exact legal root moves;
4. evaluate a newly frozen machine reference-disadvantage criterion only after candidate occurrence selection;
5. if the selected occurrence fails the disadvantage criterion, do not replace it within the trajectory;
6. collapse duplicate selected RAW states deterministically before phase quota;
7. fill phase quotas only by frozen hash rank; no favorable replacement is permitted.

The study-start default reference-disadvantage criterion is `D3 exact-root bestScore < 0` under a newly frozen PCRPR reference-search identity. Reusing this operational sign criterion does not reuse PCEM rows, audits, thresholds, grammar, or outcomes.

Exact search identity and tie-breaking must be independently revalidated in Stage 0 and frozen before Stage 1 outcome generation.

## 12. Reply-policy / continuation target family

The machine continuation family prospectively distinguishes:

```text
STRONG opponent policy
MEDIUM imperfect opponent policy
WEAK / shallow alternative opponent policy
```

The baseline candidate implementations are methodologically analogous to `P_REFERENCE_D2_BEST`, `P_MEDIUM_D1_TOP3`, and `P_SHALLOW_UNIFORM`, but PCRPR must own and independently validate its exact policy identities and source hashes.

For each root-move row, Stage 1 may measure fresh bounded terminal outcomes under the frozen policy family. The primary target family is **medium-vs-strong practical comeback lift**. Weak-vs-strong lift and full policy span are secondary target families.

Exact continuation horizon, replicate count, common-random-number binding, administrative cutoff handling, and target formula must be frozen before Stage 1 scientific outcome generation.

These continuation-derived quantities are outcomes/targets only and are prohibited from predictor features.

## 13. Stage 1 development firewall

Before Stage 1 outcome generation, PCRPR must freeze at minimum:

- exact source-game population and hashes;
- exact opening and generation strata;
- root disadvantage/search identity;
- root selection and no-replacement semantics;
- exact policy family and continuation instrument;
- replicate count, horizon, RNG/CRN binding, terminal accounting;
- Stage-0-eligible feature schema, order, numeric/hash contract;
- exact development model/score search space;
- grouping/cross-validation unit;
- model-selection metric and tie-break rule;
- support, phase, trajectory and class/target-variation readiness floors;
- Stage 1 decision taxonomy;
- resource ceilings and post-consumption failure behavior;
- production and independent implementation hashes;
- explicit Stage 1 authorization.

Stage 1 rows are never Stage 2 evidence.

## 14. Stage 2 firewall

Before any Stage 2 scientific outcome exists, PCRPR must commit one exact formal contract containing:

- final representation families and scalar feature order;
- normalization/imputation/missingness rules;
- frozen model/score and parameters;
- exact primary target/endpoint and acceptance threshold;
- formal support/estimability floors;
- phase-specific requirements;
- uncertainty/multiplicity rules;
- fresh Stage 2 population/seed block;
- Stage 1 overlap firewall at seed, trajectory, opening prefix, RAW state, root occurrence/history and root-move-row identity levels;
- production and independent source hashes;
- exact result hashing and comparison semantics;
- fail-closed rule;
- separate explicit Stage 2 authorization.

No Stage 2 metric may be promoted from secondary to primary after outcome inspection.

## 15. Consume-once and fail-closed rule

A scientific block becomes `CONSUMED` when the explicitly authorized runner, after validating the frozen source/spec/hash envelope, writes its execution-start record and crosses the frozen boundary immediately before first scientific game generation.

Before that boundary, technical defects may be repaired only if zero scientific seeds were generated and no scientific outcome was observed.

After the boundary:

```text
same-block repair/rerun = NOT AUTHORIZED
seed replacement = NOT AUTHORIZED
seed extension = NOT AUTHORIZED
favorable root replacement = NOT AUTHORIZED
post-hoc tolerance/threshold/model substitution = NOT AUTHORIZED
```

Population insufficiency, target-support insufficiency, resource censoring, technical verification failure, and negative scientific performance are distinct dispositions and must not be conflated.

## 16. Independent verification

Mandatory independent verification must, as far as technically feasible, independently reconstruct:

- source population generation;
- trajectory and opening-prefix identity;
- RAW identity and root selection;
- exact root-move and reply sets;
- all representation features;
- policy-distribution descriptors;
- continuation measurement and target construction;
- model fitting/scoring;
- threshold/decision calculation;
- canonical row/set/result hashes.

Importing the production PCRPR feature/model helper as the verifier is prohibited.

A required independent-verification failure fails closed under the prospectively frozen taxonomy.

## 17. Decision taxonomy constraint

Scientific/terminal labels must be drawn from the current Research Generation 2 vocabulary and must distinguish at least:

```text
CONFIRMED-WITHIN-FROZEN-REPRESENTATION-AND-POPULATION
NOT-CONFIRMED
INCONCLUSIVE
NON-ESTIMABLE
STAGE0-TECHNICAL-FAILED
STAGE1-DEVELOPMENT-BLOCKED
STAGE1-TECHNICAL-INVALID
TECHNICAL-INVALID
RESOURCE-CENSORED
NOT-AUTHORIZED-NOT-EXECUTED
```

Exact conjunctions and thresholds are frozen before the corresponding outcome generation.

## 18. Interpretation boundary

Even a positive PCRPR result would establish only reproducible machine-operational reply-pressure/practical-comeback structure within the frozen RAW population, search/policy family, continuation horizon, representation, model and endpoint.

It would not establish game-theoretic optimality, true Bao winning probability, a universal winning-try taxonomy, human error probability, human difficulty/deception/psychological pressure, expert/traditional recognition, causal mechanism, public-AI improvement, or full-game solution.
