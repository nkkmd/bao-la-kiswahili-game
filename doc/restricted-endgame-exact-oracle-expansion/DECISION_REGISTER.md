# REEOE-STUDY1 — Decision Register

Updated: 2026-08-28

The decisions below are prospective unless explicitly identified as later closure records. Study-start decisions are immutable after relevant scientific outcome inspection.

## D-001 — Study identity

`G2-04` is instantiated as a new independent Research Generation 2 Study.

```text
Formal title = Restricted Endgame Exact Oracle Expansion Study 1
Study ID = REEOE-STUDY1
Agenda label = G2-04
```

`G2-04` is an agenda sequence label, not the formal Study ID.

## D-002 — Study-start baseline

```text
expected prior main = aba61596e6440e9d54be6f1e9520f65e983000b3
observed remote main = aba61596e6440e9d54be6f1e9520f65e983000b3
match = true
open PRs = 0
competing active Generation 2 research = false
```

Residual G2 branches had zero commits ahead of `main` and do not represent unmerged active studies.

## D-003 — Pure-science boundary

REEOE-STUDY1 is pure Research Generation 2 science. Public Bao AI strength, deployment, latency, product quality, and AI-GEN promotion are not scientific endpoints.

## D-004 — Immutable G2-01 closure

```text
PEOCR-STUDY1 = INCONCLUSIVE
```

No exact-oracle evidence may change or rescue that closure.

## D-005 — Immutable G2-02 closure

```text
SRDR-STUDY1 = INCONCLUSIVE
primaryFormalCriterion = null
uniqueHistoricalTrajectoriesAfterStage1Firewall = 1040 < 1050
```

The 10-trajectory shortfall may not be repaired using G2-04 generation or identity rules.

## D-006 — Immutable G2-03 closure

```text
STSCV-STUDY1 = INCONCLUSIVE
STSCV-T01-SEAT-SWAP-LOCAL = NON-ESTIMABLE
STSCV-T02-LR-MTAJI-HOUSELESS = NON-ESTIMABLE
STSCV-T03-SEAT-SWAP-LR-MTAJI-HOUSELESS = NON-ESTIMABLE
validated transform set = []
semantic-domain canonicalization = NON-ESTIMABLE
standard-start reachable-population deduplication = NON-ESTIMABLE
```

Production-only zero-mismatch diagnostics are not validated transforms.

## D-007 — Authoritative RAW state identity

Formal state identity is exactly:

```text
pits,reserve,houseOwned,player,phase,winner,pending
```

`turn` and `reason` are excluded. Missing `pending` fails closed.

## D-008 — No symmetry or canonicalization

REEOE-STUDY1 forbids:

```text
symmetry reduction
canonicalized identity
player-swap equivalence
left-right equivalence
quotient graph
orbit deduplication
symmetry-reduced state counting
```

All exact node counts and identities are RAW-only.

## D-009 — Exact move identity

Exact move identity includes:

```text
type,phase,row,index,direction,side,houseChoice,houseTwo
```

Namua variants may not be collapsed by a permissive legality helper.

## D-010 — REWR formal decision remains immutable

```text
REWR-STUDY1 = EXACT-SOLVED-WITHIN-FROZEN-DOMAIN
frozen exact domain = 8 states / 7 edges
```

The 8-state result may be a technical regression fixture but is not fresh G2-04 evidence and is not generalized to new domains.

## D-011 — Historical 423,733-state candidate is not resumed

The historical REWR one-shot candidate stopped at:

```text
states = 423,733
edges = 426,938
stop = ADMIN-CUTOFF
```

REEOE-STUDY1 does not continue the same candidate by simply increasing a cap. New formal domains require a new prospective domain-selection and resource contract.

## D-012 — ORISC closure remains immutable

```text
Axis A = ORACLE-REPRESENTATION-INTEGRITY-NOT-CONFIRMED
Axis B = NOT-AUTHORIZED-NOT-EXECUTED
validated transformation set = []
```

REEOE-STUDY1 does not rewrite historical repository-facing oracle rows.

## D-013 — SSGTC closure remains immutable

```text
SSGTC-STUDY1 = SSGTC-EXACT-WITHIN-FROZEN-DEPTH-8-DOMAIN
```

Its standard-start bounded graph is not silently converted into an endgame oracle population.

## D-014 — Stage identities

```text
REEOE-S0-TECHNICAL-2026-08-28-v1
REEOE-S1-DEVELOPMENT-2026-08-28-v1
REEOE-S2-FORMAL-2026-08-28-v1
```

Stage 0 is technical only, Stage 1 development only, Stage 2 the only formal exact-analysis decision stage.

## D-015 — No automatic Stage 1 or Stage 2 authorization

Study-start documents authorize only technical Stage 0 work. Stage 1 and Stage 2 each require a separate prospective spec and explicit authorization before their scientific generation.

## D-016 — Domain selection is outcome-blind

Formal domain selection must be determined by structural/reachability/resource rules fixed before the relevant exact outcomes are inspected. WIN/LOSS/RECURRENT proportions, cycles, DTF, optimal moves, tactical interest, or favorable winner may not drive selection.

## D-017 — Historical and synthetic domains are separate evidence classes

Synthetic fixtures are valid technical instruments. Any synthetic-valid scientific domain must be prospectively labeled as synthetic and must not be reported as historically reachable evidence.

## D-018 — Complete forward closure is mandatory

Every exact-eligible nonterminal state must have its complete exact legal-move set and all successor RAW states represented. A root filter may not drop a legal successor.

## D-019 — Incomplete closure is not exact

The Study distinguishes at least:

```text
CLOSED
OPEN / ESCAPING
INCOMPLETE-DOMAIN
RESOURCE-CUTOFF
TECHNICAL-INVALID
NON-ESTIMABLE
```

Near-complete is never exact.

## D-020 — Resource/administrative cutoff is not a game result

Resource exhaustion, workflow timeout, administrative cutoff, artifact truncation, or implementation safety cutoff may not be reinterpreted as WIN, LOSS, DRAW, or RECURRENT.

## D-021 — Terminal semantics use absolute winner

Terminal base cases use exact terminal semantics and absolute winner. Post-terminal `state.player` alone does not define terminal WIN/LOSS.

## D-022 — No automatic DRAW label

At Study start the exact fixed-point vocabulary is `TERMINAL`, `WIN`, `LOSS`, `RECURRENT`. Current frozen rule semantics do not authorize converting any cycle or unresolved recurrent region into formal `DRAW` without a separately prospectively established draw rule.

## D-023 — DTF metric

Default exact distance is legal-move/ply distance-to-forced-terminal:

```text
TERMINAL = 0
WIN = 1 + min DTF(winning successors)
LOSS = 1 + max DTF(all opponent-winning successors)
RECURRENT = null
```

No post-outcome distance redefinition is authorized.

## D-024 — All optimal moves are required

Resolved states must preserve the full optimal/max-resistance move set and its multiplicity, not merely one representative best move.

## D-025 — SCC structure is an independent graph endpoint

Strongly connected components are computed from the exact graph independently of retrograde labels. Cyclic SCC presence and unresolved recurrent classification are distinct facts.

## D-026 — Positive technical control

`REEOE-C00-REWR-8STATE-REGRESSION` must reconstruct the prior exact fixture and reproduce node/edge identities, predecessor relation, values, DTF, and all optimal/max-resistance move sets. It is instrument evidence only.

## D-027 — Negative technical controls

The independent verifier must detect:

```text
REEOE-C01-MISSING-SUCCESSOR
REEOE-C02-INCORRECT-TERMINAL
REEOE-C03-INCOMPLETE-EDGE-SET
REEOE-C04-CORRUPTED-PREDECESSOR
```

A false pass is an instrument failure.

## D-028 — Independent verifier is mandatory

Production and independent verification must separately reconstruct RAW serialization, state validity, legal moves, successors, terminal/winner semantics, node/edge sets, predecessors, closure, SCCs, retrograde values, DTF, optimal moves, and decision inputs wherever feasible.

## D-029 — Prefer algorithmic diversity

Production and independent retrograde/graph verification should use different algorithmic paths. Any unavoidable shared rule dependency must be disclosed before Stage 1/2 scientific authorization.

## D-030 — Resource contract must be frozen before Stage 2

Stage 2 requires outcome-preceding limits for states, edges, deterministic work units where possible, RSS, artifacts, sharding, checkpoints, restart semantics, administrative cutoff, and resource-cutoff classification.

## D-031 — Hash/provenance minimum

Scientific stages track at least spec, source, authorization, domain-definition, root/population, RAW-identity, node-set, edge-set, closure, retrograde-result, verification, canonical-result, and artifact-ZIP hashes.

## D-032 — Large raw graphs are not automatically committed

Large graphs belong in immutable workflow artifacts unless repository storage is necessary. Repository-facing records remain compact and hash-bound.

## D-033 — Candidate formal decision taxonomy is provisional until Stage 2 freeze

Candidate domain-level labels are:

```text
EXACT-SOLVED-WITHIN-FROZEN-DOMAIN
NOT-EXACTLY-SOLVED
NON-ESTIMABLE
```

Stage 2 must prospectively finalize the exact mapping between scientific labels and technical/reproducibility failures.

## D-034 — Technical failure remains separate from scientific domain property

Solver/verifier disagreement, source/hash drift, incomplete closure, or resource cutoff is not automatically scientific `NOT-EXACTLY-SOLVED`. The frozen Stage 2 failure rule controls and must fail closed.

## D-035 — No rescue

After relevant outcome inspection, no domain shrinkage, root/state replacement, cycle exclusion, seed extension, cap increase, edge omission, closure-definition change, DRAW/RECURRENT redefinition, DTF change, favorable subset, alternate solver substitution, threshold relaxation, near-complete relabeling, subgroup rescue, or symmetry/canonicalization introduction is authorized.

## D-036 — Technical correction boundary

A genuine implementation defect found before scientific outcome may be corrected prospectively with explicit revoke/refreeze/reauthorize provenance. A defect discovered after scientific outcome must be classified under the frozen failure rule; a favorable same-evidence rerun is not presumed permissible.

## D-037 — Stage 0 first task

The first technical task is to audit current RAW serialization, move identity, guard-free endgame transition, complete closure traversal, retrograde implementation, and independent verifier; then reproduce the positive fixture and demonstrate detection of all four corruption controls.
