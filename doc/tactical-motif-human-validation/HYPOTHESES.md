# HYPOTHESES — Tactical Motif Human / Expert Validation Study 1

Updated: 2026-08-18  
Status: **STUDY 1 CLOSED / HUMAN HYPOTHESES NOT ESTIMABLE (N=0)**

## Final disposition

The hypotheses below were defined prospectively, but the human-facing phase was never launched. Scientific recruitment started = `false`, formal human responses = `0`, and included primary experts = `0` against the frozen minimum of `10`.

Therefore:

```text
TMHV-H1 = NOT-EVALUATED / INCONCLUSIVE-NOT-ESTIMABLE
TMHV-H2 = NOT-EVALUATED
TMHV-H3 = NOT-EVALUATED
TMHV-H4 = NOT-EVALUATED
```

No human p-value, effect estimate, score distribution, label frequency, or explanation coding result exists. This does not constitute negative human evidence.

## TMHV-H1 — Primary: cross-position transfer recognition

Prospective hypothesis:

Bao experts exposed to blinded, position-only formal stimuli would identify C03-C03 cross-position pairs as sharing a reusable move-selection principle more consistently than expected under the participant-level null.

Planned participant-level quantity:

```text
primaryScore_i = correct primary blocks / usable primary blocks
participantSuccess_i = 1 iff primaryScore_i > 0.5
```

Planned formal hypothesis:

```text
H0: P(participantSuccess=1) <= 0.5
H1: P(participantSuccess=1) > 0.5
```

This was a population-level expert-recognition hypothesis, not an independence assumption over individual position responses.

**Final disposition:** not evaluated because `N=0 < 10`; human axis = `INCONCLUSIVE-NOT-ESTIMABLE`.

## TMHV-H2 — Secondary: move-choice recognition

Prospective hypothesis:

On blinded individual C03-eligible positions not used in H1, experts would select a move matching the frozen C03 move abstraction at a rate materially above prospectively defined comparison/chance expectations.

This endpoint could not substitute for H1.

**Final disposition:** not evaluated; no human responses were collected.

## TMHV-H3 — Secondary: explicit reusable-principle / tesuji labeling

Prospective hypothesis:

After uncued primary and move-choice tasks, experts would classify the C03 structure as a reusable local tactical principle or tesuji more often than as a position-specific best move, joseki-only move, or coincidental similarity.

**Final disposition:** not evaluated; no label task was administered.

## TMHV-H4 — Exploratory: explanation agreement

Prospective exploratory question:

Expert explanations might contain concepts corresponding to the frozen machine construct, including low reusable-pit structure, rightward row-1 takata morphology, preservation/transformation of nyumba-related seed structure, or explicit cross-position transfer rationale.

Unless an independent blinded coding workflow were available and frozen before human data, H4 would remain exploratory.

**Final disposition:** not evaluated; no free-text responses were collected.

## Valid-result boundary

The prospective design explicitly allowed positive, negative, heterogeneous, or non-estimable outcomes. The realized result was the non-estimable branch:

```text
required included primary experts = 10
observed included primary experts = 0
formal human inference performed = false
final human axis = INCONCLUSIVE-NOT-ESTIMABLE
```

None of this changes the historical machine decision `TM-S2-C03 = CONFIRMED`.