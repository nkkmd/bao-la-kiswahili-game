# RRCLGR-STUDY1 — Decision Register

Updated: 2026-09-03

## D1 — Study authorization

Decision: `PREREQUISITE-REQUIRED` after post-G3-09 G3-10 authorization review.

RRCLGR-STUDY1 was created as a new prospective independent pre-G3-10 prerequisite. G3-09 `CLGR-STUDY1` remained closed and unchanged.

## D2 — Frozen representation

Decision: use only `RRCLGR-R1-EXACT-SQUASHED-L1` over six exact LGTGMIV F1–F5-derived axes, relative RAW horizon 5, with exact rational arithmetic, equal-weight L1 and k=3 tie-inclusive neighborhood.

No representation-family search, learned weighting, phase-specific scaling, outcome-dependent axis choice or post-fresh redesign was authorized.

## D3 — Resource robustness

Decision: separate scientific candidate identity from deterministic bounded depth-5 preflight eligibility. Resource eligibility was to be determined before coordinate generation, with production/independent agreement and fail-closed behavior.

## D4 — Stage 0

Decision: `RRCLGR-S0-TECHNICAL-2026-09-03-v1 = STAGE0-PASS`.

Stage 0 was technical-only and did not authorize Stage 1 automatically.

## D5 — Stage 1 authorization

Decision: `RRCLGR-STAGE1-AUTHORIZED` after separate post-Stage-0 review.

Exactly one Stage 1 scientific execution was authorized. Stage 2 and G3-10 remained disabled.

## D6 — Stage 1 execution

GitHub Actions run `33759611989` was the single authorized Stage 1 execution. Fresh Stage 1 seed access occurred.

Canonical disposition:

**`STAGE1-TECHNICAL-INVALID`**

Candidate manifest did not complete. Scientific summary was not authorized.

## D7 — No rerun

Decision: **NO SAME-EVIDENCE RERUN**.

The causal defect was a post-fresh implementation type error: candidate identity-row arrays were passed directly to the inherited low-level digest rather than canonicalized before SHA-256 input. Repair followed by rerun of `32010001..32010256` would violate the frozen no-rescue rule.

## D8 — Formal Study closure

Decision:

**`RRCLGR-STUDY1 = CLOSED / TECHNICAL-INVALID`**

Formal continuous-representation eligibility remains `NOT ESTABLISHED`.

Stage 2 is `NOT-AUTHORIZED / NOT-EXECUTED`.

## D9 — G3-10 downstream state

Decision: G3-10 remains **`NOT AUTHORIZED`**.

RRCLGR Stage 1 does not establish the coordinate dependency required for G3-10.

## D10 — Successor boundary

A successor may be created only as a new prospective independent prerequisite Study/version. Requirements include:

- fresh scientific seed namespace;
- no RRCLGR scientific measurement reuse;
- no RRCLGR Stage 1 seed reuse;
- no favorable root targeting from RRCLGR fresh access;
- new source-bound authorization;
- pre-fresh technical test of candidate-core canonical serialization;
- independent production/verifier implementations;
- protected depth-10 remains sealed;
- no main integration without explicit user instruction.
