# BECT-STUDY1 — Stage 0 technical authorization

Date: 2026-09-02  
Decision: **`STAGE0-AUTHORIZED / TECHNICAL-ONLY / EXACTLY ONE FORMAL EXECUTION`**

## Authorized Stage

```text
Study = BECT-STUDY1
Stage = BECT-S0-TECHNICAL-2026-09-02-v1
authorization nonce = BECT-S0-AUTH-2026-09-02-V1-01
max formal Stage 0 executions = 1
fresh scientific evidence = PROHIBITED
Stage 1 seed access = PROHIBITED
Stage 2 seed access = PROHIBITED
protected depth-10 access = PROHIBITED
```

## Preconditions

The post-G3-04 program review concluded `G3-05-AUTHORIZED`. The BECT Study ID, titles, Stage IDs, measurement family bindings, longitudinal endpoint universe, event grammar, populations, fresh seed namespaces, firewall, inference rules, resource ceilings, equality semantics, execution-integrity rules and no-rescue rules have been frozen before fresh scientific evidence.

## Authorized technical inputs

Stage 0 may use only:

- synthetic exact level/delta/event fixtures;
- synthetic ordinary-object/null-prototype equality controls;
- engine/rule semantics bound by the frozen source blob;
- one explicitly non-scientific technical replay seed `31500001`, which is outside Stage 1/2 fresh namespaces and is permanently prohibited from later scientific use;
- bounded relative depth-5 local reconstruction for technical roots from that technical replay only.

Stage 0 must not read scientific result JSON from G3-03/G3-04 except static source/code bindings already declared in the frozen protocol. It must not inspect G3-05 Stage 1/2 source populations.

## Mandatory gates

The formal technical execution must verify:

1. deterministic trajectory replay;
2. adjacent-ply successor/root identity binding;
3. ordered root extraction and overlapping-window semantics;
4. repeated root/subgraph identity handling without treating time windows as independent samples;
5. exact rational level/delta arithmetic;
6. no-change, expansion, compression, persistence, reversal, stall/recovery, reopening and extinction fixtures;
7. trajectory/order invariance;
8. production/independent canonical scientific equality;
9. successful canonical equality even under incidental JavaScript prototype difference, while prototype-sensitive deep equality is demonstrated as a negative control rather than a gate;
10. implementation separation;
11. no fresh scientific seed access;
12. protected depth-10 firewall.

## Execution integrity

The formal Stage 0 execution must use one dedicated trigger path and a concurrency guard. It must bind the authorized source blobs and preregistration. The scientific/technical result must be durably uploaded before repository mirroring. Any mirror operation must use the exact generated bytes; recomputation for artifact recovery is prohibited.

If the formal Stage 0 execution reaches fixture computation and fails, do not silently rerun it. Record the technical disposition and conduct a new governance review if another Stage 0 version is required. A pre-computation trigger/source-binding failure that consumes no fixture or scientific evidence may only be corrected prospectively under an explicitly versioned technical refreeze.

## Stage progression

A Stage 0 PASS establishes technical readiness only. It **does not authorize Stage 1**. After Stage 0 closure, a separate post-Stage-0 Stage 1 authorization review is required before any access to `31510001..31510240`.
