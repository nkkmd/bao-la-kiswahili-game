# 2026-08-31 — G3-01 measurement foundation technical-invalid closure

## Decision

Research Generation 3 `G3-01`として実施した`LGTGMF-STUDY1`を、正式判断 **`TECHNICAL-INVALID`** で閉じる。

```text
Study = LGTGMF-STUDY1
Stage 0 v1 = STAGE0-TECHNICAL-INVALID
Stage 0 v2 = STAGE0-TECHNICAL-PASS
Stage 1 = STAGE1-TECHNICAL-INVALID
Stage 2 = NOT-AUTHORIZED-NOT-EXECUTED
formal eligible measurement families = []
Stage 1 seed consumption = 31010001..31010096
Stage 2 seed consumption = NONE
```

## Basis

Stage 1 fresh developmentでは、prospectively frozen populationからNamua 6 / Mtaji 6の全12 rootsを選択し、production / structurally independent implementationが全rootをrelative depth 5までcomplete reconstructionした。

root identity、source identity、root-level measurement core、F1〜F5のfamily digestはすべてexact一致した。一方、Study開始時に必須としたdeterministic canonical stage manifestについて、implementationがelapsed time / RSS等のnon-deterministic resource observationsを`stageCoreSha256` inputへ含めたためstage-level digestが一致しなかった。

fresh Stage 1 evidence生成・read後にこのimplementationを修正し、同じseed blockをrerunして判定を変更することはprospectively frozen no-rescue ruleに反する。そのためroot-level agreementをformal eligibilityへ救済せず、Stage 1をtechnical-invalidとしてfail-closedする。

## Downstream dependency

Research Generation 3 program planでは、G3-02〜G3-08は原則としてG3-01でformal eligibilityを得たmeasurement familyだけを使用する。

本Studyのformal eligible family setは空なので:

```text
G3-02..G3-08 automatic execution = BLOCKED
```

とする。

これはG3-02〜G3-08各agenda questionのscientific negative closureではない。それらを開始するためのmeasurement dependencyが現在成立していないというprogram-level authorization boundaryである。

次に進む場合、G3-01をreopen / rescueせず、今回のfailure modeをdesign informationとして利用した新しいprospective measurement-instrument prerequisite Studyを設けるか、Research Generation 3 dependency graphを別のprogram decisionで再設計する必要がある。

## Protected evidence

standard initial RAW rootのcomplete depth-10 exact layerはG3-11用にsealedしたまま維持する。

```text
complete depth-10 enumeration generated by G3-01 = false
depth-10 scientific geometry/count read by G3-01 = false
G2-12 estimator used as holdout truth/input = false
```

## Immutable upstream

Research Generation 2のclosed resultは変更しない。特に:

```text
G2-05 = EXACT-WITHIN-FROZEN-DEPTH-9-DOMAIN
validated transform set = []
G2-11 = NON-ESTIMABLE / NOT-AUTHORIZED-NOT-EXECUTED
G2-12 = TECHNICAL-INVALID / selectedEstimator = null
whole-Bao state-space estimate = NOT AUTHORIZED
whole-Bao game-tree estimate = NOT AUTHORIZED
```

を維持する。

## Main integration

このclosureはG3-01 research branch上の決定である。`main`への統合はuserの明示的指示があるまで行わない。
