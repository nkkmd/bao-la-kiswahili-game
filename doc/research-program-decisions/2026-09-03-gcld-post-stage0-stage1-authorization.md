# GCLD-STUDY1 post-Stage-0 Stage 1 authorization review

Date: 2026-09-03

## Formal decision

**`GCLD-STAGE1-AUTHORIZED`**

## Basis

Stage 0 `GCLD-S0-TECHNICAL-2026-09-03-v1` completed exactly once in GitHub Actions run `33766979732` and produced `STAGE0-PASS`.

Technical gates established before any GCLD scientific fresh access:

- exact C1..C5 synthetic endpoint agreement;
- exact expected-value fixtures;
- 32 endpoint-preserving deterministic temporal controls;
- exact control median agreement;
- exact two-sided sign-test arithmetic;
- exact Holm arithmetic;
- real technical trajectory generation through ply 72;
- complete 15-checkpoint preflight agreement;
- exact CRCLGR coordinate reconstruction and trajectory-level aggregation;
- terminal-before-72, source relay-limit and resource-limit fail-closed controls;
- production/independent implementation separation;
- trajectory as the only inferential experimental unit;
- no checkpoint/window inferential pseudo-replication.

Stage 0 accessed technical seeds only. `freshScientificSeedAccess=false`, Stage 1/2 seed access was false, and protected depth-10 access was false.

The canonical Stage 0 artifact was exact-byte mirrored; repository Git blob identity is `650dd95dba0d246c28de2970c09cc1fe3b843f0d`.

## Authorization boundary

Stage 1 `GCLD-S1-DEVELOPMENT-2026-09-03-v1` may be executed exactly once only after its runner, independent verification boundary, source bindings, upstream identity firewall and durable-lease workflow are frozen.

Frozen Stage 1 population contract remains:

```text
seed block = 32210001..32210256
candidate trajectories = 24
minimum fully resource-eligible trajectories = 18
measured development trajectories = first 16 eligible in frozen candidate order
checkpoints per trajectory = 15
minimum defined contrasts per endpoint = 14/16
```

Stage 2 promotion may use only Stage 1 technical/readiness gates. Endpoint signs, effect magnitudes, p-values or favorable scientific patterns may not be used as a Stage 2 promotion criterion.

```text
Stage 1 = AUTHORIZED FOR SEPARATE SOURCE-BOUND EXACTLY-ONE EXECUTION
Stage 2 = NOT AUTHORIZED
protected depth-10 = SEALED / NOT GENERATED / NOT READ / NOT PEEKED
main integration = NOT AUTHORIZED / NOT PERFORMED
```
