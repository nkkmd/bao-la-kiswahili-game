# 2026-09-04 — post-G3-10 current-state G3-11 authorization review

## Decision

**`G3-11-AUTHORIZED`**

Research Generation 3 `G3-11` may begin as a new, prospective, independent Study only under the narrowed scope and pre-access conditions fixed by this review.

This decision does **not** authorize reading, generating, partially probing, estimating from, or otherwise opening the protected standard-initial-RAW-root complete exact depth-10 scientific outcome before the Study/Stage/spec/resource/source/authorization freeze is durably materialized.

## 1. Current source identity

Reviewed repository:

```text
repository = nkkmd/bao-la-kiswahili-game
source branch = main
source SHA = e537199a959c0808cbef6cf8aaeb1caab91e3702
review date = 2026-09-04
```

The current `main` SHA was re-read immediately before branch creation and matched the integration-era reference SHA supplied by the user.

G3-11 research branch:

```text
research/g3-11-fresh-depth10-exact-geometry-holdout
```

The branch was created from the exact reviewed `main` SHA. `main` integration is not authorized by this review.

## 2. Reconstructed post-G3-10 state

G3-10 / `GCLD-STUDY1` is closed as:

```text
CLOSED / FORMAL-COMPLETE
C1 = CONFIRMED / ACTUAL-GREATER
C2 = CONFIRMED / ACTUAL-GREATER
C3 = CONFIRMED / ACTUAL-LESS
C4 = NOT-CONFIRMED
C5 = CONFIRMED / ACTUAL-GREATER
canonical scientific-result SHA-256 = c5ec84cecb4e540ce7ad9f52548dac14deecde3423b2f4d10e1c39e1000ae09f
main integration = COMPLETE / FAST-FORWARD / force=false
```

No G3-10 endpoint is automatically promoted into G3-11.

A current-facing documentation inconsistency was detected: `doc/FUTURE_RESEARCH_AGENDA.md` still contains a pre-integration G3-10 `main integration NOT AUTHORIZED / NOT PERFORMED` statement, while G3-10 `CURRENT_STATUS.md`, Research Generation 3 `README.md` / `CURRENT_STATUS.md`, the integration checkpoint, and current `main` provenance record integration as complete. This is classified as a documentation-synchronization defect, not a scientific-state ambiguity. It must be corrected on the G3-11 research branch before protected depth-10 access. Historical `doc/research-generation-3/PROGRAM_PLAN.md` remains immutable and must not be rewritten.

## 3. Protected depth-10 integrity review

Outcome-blind repository records reviewed for G3-10, LGTGMIV, CRCLGR, Research Generation 3 current status, the historical program plan, and the G3-10 integration checkpoint consistently record:

```text
Protected standard-root complete exact depth-10 holdout =
SEALED / NOT GENERATED / NOT READ / NOT PEEKED
```

During this authorization review:

- no depth-10 enumeration was generated;
- no partial depth-10 enumeration was generated;
- no depth-10 state count, tree count, branching, transposition, reconvergence, geometry measurement, or resource probe was requested;
- no Actions artifact, abandoned branch, cache, historical G2-12 output, or other path was searched for a depth-10 scientific outcome;
- no G2-12 estimator output was used as a scientific prior, target, expected count, or resource-fitting input.

The protected-evidence integrity decision is therefore:

**`SEALED-INTEGRITY-ACCEPTED-OUTCOME-BLIND`**

This is a repository-provenance conclusion, not an impossible omniscient claim about data outside the reviewed repository records.

## 4. Dependency review

### 4.1 Authoritative state and move identity — PASS

Authoritative scientific state identity remains RAW-only:

```text
pits,reserve,houseOwned,player,phase,winner,pending
```

Excluded from state identity:

```text
turn,reason
```

Exact move identity remains:

```text
type,phase,row,index,direction,side,houseChoice,houseTwo
```

### 4.2 Transform / canonicalization boundary — PASS

```text
validated transform set = []
symmetry reduction = NOT AUTHORIZED
canonicalization-based state collapse = NOT AUTHORIZED
```

G3-11 must enumerate authoritative RAW states directly.

### 4.3 Local geometry measurement dependency — PASS WITH BOUNDARY

The independent prerequisite `LGTGMIV-STUDY1` remains:

```text
CLOSED / FORMAL-ELIGIBLE-ALL
```

Formal-eligible families:

1. `LGTGMIV-F1-TREE-OCCURRENCE`
2. `LGTGMIV-F2-RAW-GRAPH`
3. `LGTGMIV-F3-TRANSPOSITION-RECONVERGENCE`
4. `LGTGMIV-F4-TREE-GRAPH-RELATION`
5. `LGTGMIV-F5-REPLY-GEOMETRY`

That eligibility is bounded to the original RAW-only relative-depth-5 instrument contract and is **not** automatically extended to depth 10. G3-11 therefore uses the eligible definitions as construct vocabulary while requiring a new complete depth-10 production enumeration plus materially separate full independent enumeration. No claim of “LGTGMIV validity at depth 10” is assumed before G3-11 outcome.

### 4.4 Continuous representation dependency — NOT REQUIRED

`CRCLGR-R1-EXACT-SQUASHED-L1` is formally eligible within its resource-bounded relative-depth-5 population, but G3-11 does not require continuous representation. The protected target is the complete authoritative RAW exact domain and exact geometry primitives. Introducing continuous coordinates would add an unnecessary representation layer and would weaken the direct exact-holdout design.

### 4.5 Exact enumeration architecture — PASS

G2-05 established a complete independently reproduced standard-root RAW domain through depth 9 and provides historical exact reference plus operational telemetry. It may be used for metric design, technical fixtures, and resource planning only.

The post-G2-05 hardening requirements H1–H5 apply prospectively to G3-11, especially:

- independent regeneration of every claimed-complete layer;
- final ambient/resource recheck after final scientific materialization and before exact-complete classification;
- mandatory pre-formal negative-control matrix;
- new source identity and authorization;
- no silent promotion of a resource-censored prefix.

G2-12 estimation/extrapolation evidence is excluded.

## 5. Construct compatibility / target validity

G3-11 must not indiscriminately “reconfirm G3-01..G3-10”. Current upstream outcomes materially narrow what can be tested by one standard-root depth-10 exact domain.

### 5.1 Directly compatible with the depth-10 exact domain

The following are compatible as deeper exact reconstruction targets:

- complete layer-wise RAW state sets and cumulative RAW set;
- complete parent-layer legal transition sets through parent depth 9;
- tree occurrence propagation through depth 10;
- transposition / multi-parent / duplicate-arrival accounting at child depth 10;
- tree/RAW relation and exact set/count/hash identity;
- exact parent-depth-9 legal-width / branching distribution as a descriptive primitive.

These correspond primarily to the F1–F4 geometry construct families and the legal-width primitive of F5, but the new G3-11 exact-domain contract, not an assumed horizon-generalization of LGTGMIV, is authoritative.

### 5.2 Derived exact comparison only — not a prior Study re-decision

The following may be calculated or compared only as new G3-11 exact descriptors:

- the cumulative tree/RAW ratio metric used in G3-04 C6;
- raw components related to CRCLGR A2–A5;
- branching / duplicate-transition / transposition descriptors appearing in earlier G3 candidate families.

Their depth-10 values do **not** re-test the original phase-paired, resource-bounded, or search-conditioned inferential claims.

### 5.3 Construct mismatch — excluded from G3-11 formal target

- G3-04 C1/C6 original formal claims are paired Namua-vs-Mtaji relative-depth-5 phase contrasts. A single standard-root depth-10 domain is not that experimental design.
- G3-07 confirmed candidates concern association between local geometry strata and search-output changes under peer search-condition perturbations. Exact enumeration alone does not instantiate that construct.
- CRCLGR representation eligibility concerns cross-root continuous-coordinate reproducibility/nondegeneracy under a resource-bounded pre-root contract. One deeper standard-root exact domain is not a representation-validation population.
- G3-10 C1–C5 are trajectory-level chronology contrasts against endpoint-preserving order-destroyed controls. A single exact depth layer/domain has no equivalent trajectory/control structure.
- G3-01, G3-02, G3-03, G3-05, G3-06, G3-08, and G3-09 do not contribute positive formal claim targets where their relevant Study state is `TECHNICAL-INVALID`, empty-promotion, or otherwise non-eligible.

No existing upstream `CONFIRMED`, `NOT-CONFIRMED`, `NON-ESTIMABLE`, `TECHNICAL-INVALID`, or `INCONCLUSIVE` decision may be changed by G3-11.

## 6. Prospective G3-11 formal target family

The Study may be frozen around one exact-domain endpoint plus four narrow deeper-holdout checks chosen before depth-10 access from the immutable depth-0..9 reference and eligible G3 geometry vocabulary.

### E0 — complete exact depth-10 domain

Required:

```text
root = standard initial RAW state
target depth = 10
complete reachable layers = 0..10
complete parent expansion layers = 0..9
RAW-only = true
symmetry reduction = false
canonicalization collapse = false
```

### H1 — exact-depth novelty continuation

Historical depth 0..9 property:

```text
newRawStateCount[d] == uniqueRawStateCount[d]
```

Frozen depth-10 check:

```text
newRawStateCount[10] == uniqueRawStateCount[10]
```

### H2 — layer tree/RAW divergence continuation

Frozen depth-10 check:

```text
treeNodeOccurrences[10] > uniqueRawStateCount[10]
```

### H3 — cumulative tree/RAW inflation continuation

Frozen directional check:

```text
cumulativeTreeToRawRatioThrough10 > cumulativeTreeToRawRatioThrough9
```

Comparison must be performed as exact rational cross-products, not floating-point tolerance.

### H4 — transposition persistence at the fresh layer

Frozen depth-10 check:

```text
duplicateArrivalCount[10] > 0
AND statesWithMultiplePredecessors[10] > 0
```

These H1–H4 checks are new prospective G3-11 deeper exact holdout tests. They do not overwrite the inferential decisions of earlier Studies.

## 7. Technical feasibility review

No depth-10 probe is needed to find the Study technically feasible enough to authorize.

Known historical operational evidence available before depth-10 access includes:

- complete G2-05 depth-0..9 production and full independent recomputation;
- G2-05 production resource telemetry of approximately 22.9 s elapsed, approximately 680 MB peak RSS, and approximately 84 MB final uncompressed scientific artifact for the frozen depth-9 domain;
- exact known depth-9 parent layer size from the immutable depth-0..9 result;
- existing production/independent enumeration architecture;
- later mandatory enumeration hardening requirements.

These support a resource-bounded attempt without using any G2-12 growth estimator or depth-10 partial probe.

The formal resource ceiling must be frozen before protected access. Recommended ceiling for the formal spec:

```text
max cumulative distinct RAW states = 2,000,000
max depth-labelled edges = 12,000,000
max parent expansions = 2,000,000
max move evaluations = 12,000,000
max cumulative tree node occurrences = 50,000,000,000
max resident set bytes = 6,442,450,944 (6 GiB)
max wall clock seconds = 5,400
max uncompressed scientific artifact bytes = 1,073,741,824 (1 GiB)
```

These are administrative/engineering ceilings, not expected scientific values.

Any cap crossing is fail-closed and cannot be relaxed after depth-10 outcome is known.

## 8. Required Study design after authorization

The formal Study must freeze before depth-10 access:

```text
Study ID = FDEGHV-STUDY1
English title = Fresh Depth-10 Exact Geometry Holdout Validation Study 1 — Independent deeper exact validation of third-generation local game-tree geometry using the sealed standard-initial-RAW-root depth-10 domain in Bao
Japanese title = Bao standard root depth 10のfresh RAW exact enumerationによる局所ゲーム木幾何holdout検証 — sealed deeper exact domainによる第三世代geometry primitiveと事前固定continuation targetの独立検証
Stage 0 = FDEGHV-S0-TECHNICAL-2026-09-04-v1
Stage 1 = FDEGHV-S1-FORMAL-HOLDOUT-2026-09-04-v1
```

Stage 0 is technical-only and must not read or generate depth-10 scientific evidence. It must validate source binding, synthetic controls, hardening H1/H2 behavior, cap failure paths, exact-rational target logic, and production/independent structural separation.

Stage 1 is the only scientific holdout stage and may have exactly one authorized fresh scientific execution.

## 9. Independent verification contract

The formal holdout requires a materially separate independent implementation that does not import the production enumerator, production serializer, or production scientific aggregation logic.

At minimum it must independently reconstruct and compare:

- root RAW identity;
- exact RAW-state set for every layer 0..10;
- cumulative RAW-state set;
- exact legal transition / source-move-successor relation for parent layers 0..9;
- per-layer and cumulative tree occurrences;
- duplicate-arrival and predecessor/multi-parent accounting;
- exact parent-depth-9 legal-width distribution;
- per-layer and cumulative set/edge digests;
- H1–H4 scientific target values and decisions.

An exact formal label requires full independent depth-10 re-enumeration. Production-only completion is insufficient.

## 10. Decision rule and stopping rule

### Exact completion

If the complete depth-10 domain finishes inside the frozen resource ceiling, all final hardening checks pass, and production/independent results agree exactly:

**`EXACT-WITHIN-FROZEN-DEPTH-10-DOMAIN`**

Each H1–H4 target is then labeled independently:

- condition true: `DEEPER-CONFIRMED`
- condition false: `COUNTEREXAMPLE-BOUNDARY`

No p-value is needed because the endpoint is the complete frozen exact layer/domain rather than a sample from that domain.

### Resource/admin cutoff

If completion is prevented by a frozen resource/admin ceiling and integrity remains valid:

**`NON-ESTIMABLE`**

Partial layers/rows remain diagnostic provenance only and may not be promoted to formal geometry evidence.

### Technical/integrity defect

If mandatory source binding, enumeration integrity, final ambient/resource classification, artifact integrity, or production/independent exact agreement fails:

**`TECHNICAL-INVALID`**

### No-rescue

After first protected depth-10 scientific access:

- no resource-ceiling increase;
- no endpoint change;
- no subset claim;
- no replacement execution under the same Study/version;
- no same-evidence repair/rerun;
- no depth 11 access;
- no G2-12 prediction-based reinterpretation.

Depth 11+ requires a separate future prospective Study.

## 11. Authorization conclusion

Dependencies are sufficient, the protected holdout is accepted as intact at the outcome-blind repository-provenance level, a scientifically compatible narrowed target exists, and complete depth-10 exact enumeration is technically feasible enough to attempt under a prospectively fixed fail-closed ceiling.

Therefore:

# `G3-11-AUTHORIZED`

Authorization is effective only after the prospective Study/Stage/spec/source/resource/independent-verification/lease artifacts are durably frozen on the research branch. Protected depth-10 access remains prohibited until those preconditions are materialized and audited.
