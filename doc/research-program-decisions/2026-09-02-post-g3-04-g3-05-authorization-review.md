# Program Decision — Post-G3-04 G3-05 authorization review

Date: 2026-09-02  
Review baseline remote `main`: `99eb6f571dbd1e1a41f12c65c2efb7c62930de45`

## Formal decision

**`G3-05-AUTHORIZED`**

This decision authorizes Research Generation 3 agenda item G3-05 — Branch Expansion / Compression Transition Study 1 — to proceed only to a new, prospective, independent Study-definition / preregistration freeze and technical-only Stage 0 preparation.

This decision does **not** authorize generation or reading of G3-05 fresh scientific evidence. Stage 1 remains blocked until the Study contract is frozen, Stage 0 passes, and a separate Stage 1 authorization review is recorded.

## Current scientific/program boundary

The review reconfirmed:

```text
LGTGMIV-STUDY1 = CLOSED / FORMAL-ELIGIBLE-ALL
eligible families = F1,F2,F3,F4,F5
representation = RAW-ONLY
relative local horizon = 5
validated transform set = []

TCTGD-STUDY1 = CLOSED / TECHNICAL-INVALID
formal promoted candidate set = []
Stage 2 = NOT-AUTHORIZED-NOT-EXECUTED

SFCDF-STUDY1 = CLOSED / FORMAL-COMPLETE
C1 unit-width occupancy = CONFIRMED / MTAJI-GREATER
C6 cumulative tree/RAW ratio = CONFIRMED / NAMUA-GREATER
C2-C5 = not promoted / not tested in Stage 2

protected standard initial RAW-root complete exact depth-10 holdout
= SEALED / NOT GENERATED / NOT READ
```

G3-03 diagnostic directions and technical-invalid values are prohibited as G3-05 scientific evidence, threshold inputs, transition-class selection inputs, or root-selection inputs. G3-04 C1/C6 remain immutable bounded phase-difference findings and are not G3-05 outcomes.

## Authorization review findings

1. **Existing measurement foundation is sufficient.** G3-05 can be defined using only the formally eligible LGTGMIV families F1-F5 within the RAW-only relative depth-5 boundary.
2. **G3-04 positive findings are not a prerequisite.** They may be cited only as upstream motivation/context. G3-05 must define longitudinal derivatives/events independently from eligible primitives.
3. **Independent dynamic constructs are possible.** Geometry level, adjacent-ply delta, multi-ply slope/run, event onset, persistence, reversal/recovery and reopening can be prospectively defined without re-testing C1/C6.
4. **No new measurement prerequisite Study is required.** Any new longitudinal endpoint must be an explicitly frozen deterministic function of eligible LGTGMIV primitives.
5. **No new representation prerequisite is required.** Authoritative state identity remains `pits,reserve,houseOwned,player,phase,winner,pending`.
6. **No canonicalization/graph-identity prerequisite is required or authorized.** Symmetry/reflection/seat swap/canonical quotient remain excluded; validated transforms remain `[]`.
7. **No separate resource-feasibility prerequisite Study is required.** G3-05 must freeze its own resource ceilings and validate the intended longitudinal execution path technically before fresh access.
8. **Ply-to-ply longitudinal geometry is constructible at depth 5.** A trajectory can be deterministically replayed, each eligible RAW root can be reconstructed independently at relative depth 5, and the resulting ordered exact primitive series can be differenced without changing scientific state identity.
9. **No separate trajectory-alignment instrument Study is required, but Stage 0 must validate it.** Adjacent-ply root binding, successor identity, repeated-window overlap semantics, repeated RAW roots/subgraphs, event-window ordering, exact delta arithmetic, canonical serialization and production/independent equality are mandatory Stage 0 gates.
10. **Protected depth-10 evidence is unnecessary.** G3-05 can be executed entirely with fresh trajectory populations and the eligible relative depth-5 instrument. Generation/read/peek/partial enumeration/resource-peek of the protected holdout remains prohibited.

## Required construct separation

The G3-05 protocol must preserve at least:

```text
geometry level != temporal delta/change
static phase difference != trajectory transition
geometry transition != rule phase transition
geometry transition != strategic transition
geometry transition != tactical transition
geometry transition != search transition
geometry transition != value transition
reply narrowing != game-theoretic forcing
large tree/graph change != important position
```

Known Namua→Mtaji boundaries may be reported contextually only after geometry events are defined independently. Primary transition construction must not use rule-phase labels as event ground truth.

## Required longitudinal technical gate

Before any Stage 1 fresh evidence, Stage 0 must validate, without using G3-05 fresh seeds:

- deterministic source-trajectory replay and ordered root extraction;
- adjacent root `t -> t+1` successor binding;
- RAW root identity equality under replay;
- semantics for overlapping depth-5 windows;
- repeated RAW root and repeated local-subgraph handling;
- exact signed/absolute rational delta arithmetic;
- event grammar fixtures for no-change, expansion, compression, reopening, persistence and reversal/recovery;
- trajectory/order invariance;
- canonical scientific content equality by deterministic serialization/bytes/hash, not JavaScript prototype-sensitive object equality;
- production / independent implementation separation;
- actual one-trigger → one scientific workflow-path control smoke with no fresh scientific seed access;
- durable pre-computation execution lease, source blob binding, branch-advancement allowlist and artifact-before-mirror behavior;
- protected depth-10 firewall.

A Stage 0 PASS does not automatically authorize Stage 1.

## Freshness/firewall requirements

G3-05 must use new seed namespaces. At minimum, the following are prohibited as scientific selection/threshold/class inputs:

- G3-02 selected roots, runner-local candidate summaries and all scientific result fields;
- G3-03 selected roots, diagnostic candidate directions and technical-invalid scientific result fields;
- G3-04 Stage 1/2 favorable outcome fields and candidate values;
- prior Study populations as a formal holdout substitute.

Identity-only manifests may be materialized prospectively for exclusion. Development/formal firewalls must cover RAW root identity, source trajectory identity and opening-prefix identity, and should additionally cover trajectory-segment, adjacent-root-pair and event-window identity where applicable.

## Execution-integrity requirements

Each fresh scientific Stage must have:

```text
max authorized scientific executions = 1
arming and computation = separated
single trigger path
concurrency guard
pre-computation durable lease
source blob binding
remote advancement allowlist
artifact upload before repository mirror
exact-byte recovery only
scientific recomputation for artifact recovery = prohibited
same-evidence rerun after first fresh access = prohibited
```

G3-02 and G3-03 failure modes must be represented as technical controls: duplicate execution and prototype-sensitive equality must not be accepted as valid scientific paths.

## Protected evidence

The standard initial RAW-root complete exact depth-10 holdout remains:

**`SEALED / NOT GENERATED / NOT READ`**

G3-05 must not generate, partially generate, read, peek, trial-enumerate or resource-estimate it.

## Authorization meaning

`G3-05-AUTHORIZED` permits creation of a new G3-05 research branch from the current post-decision `main`, prospective freezing of the formal Study identity/contract, and technical-only Stage 0 work.

Before any fresh Stage 1 evidence, freeze at minimum:

- formal Study ID and final English/Japanese titles;
- baseline main SHA and research branch;
- Stage IDs and separate authorization semantics;
- RAW/move identity, rule source and canonical equality;
- relative depth-5 measurement-family bindings;
- longitudinal root span and trajectory-selection contract;
- transition/event grammar and any threshold-selection rule;
- development/formal seed namespaces and population sizes;
- experimental unit and cluster/dependence handling;
- Stage 1 promotion rule;
- Stage 2 endpoint/direction/persistence freeze mechanism and exact multiplicity-aware formal test;
- firewalls, resource ceilings, technical-invalid rules, no-rescue rule and execution-integrity contract.

Fresh G3-05 Stage 1 scientific evidence remains **NOT AUTHORIZED** until a separate post-Stage-0 authorization review.