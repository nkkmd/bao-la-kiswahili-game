# PBAI-P9：標準持ち時間とWorker起動条件の検証

現在は準備中であり、新規局面を用いる正式対局はまだ実行していない。候補はP8と同じPBAI-C015-v1で、学習済みモデルを変更しない。

P8では100ms・常駐VMの固定領域で強化を確認した。公開アプリは毎手Workerを起動するため、本工程では静的JavaScript版の評価器を準備し、まず既知局面の互換性を確認する。新規対局の条件は正式実行前に別途凍結する。準備中のSPEC.jsonは未凍結であり、結果確認後の条件変更を認める記録ではない。

## 現在の成果

- 既知9局面で、毎回起動するNode Workerの基準構成・候補構成が固定深さの参照結果と一致した。
- [検証ページ](../../../tools/engineering/browser/pbai-p9/index.html)とChromium用CIを用意した。ブラウザの結果はまだ未確認である。
- 実機スマートフォン、公開環境、expert設定の性能は未確認である。

## 実行と境界

既知局面の準備試験は `node --test test/pbai-p9.test.js` で実行する。検証ページはHTTPで配信し、ブラウザで「検証を実行」を押す。CIは60件のWorker要求と18件の直接実行を検証する。結果は互換性確認であり、勝率の証拠として用いない。

本工程では公開AIをAI-GEN3のまま保持する。main統合、公開設定の切替、正式採用、世代更新は実施しない。P6〜P8の凍結記録は変更しない。
