# PBAI-C015-v1：hard限定の正式採用判断

判断ID：`PBAI-C015-HARD-ADOPTION-001`  
判断：`ADOPT`（hard限定）  
配信状態：`DEPLOYMENT-PENDING`（正式配信の照合前）  
判断日：2026年9月9日

## 判断と根拠

ユーザーの「hardについては公開AIを正式に切り替えたい」という指示と、その後の[実機確認報告](DEVICE_CHECK.md)に基づき、候補`PBAI-C015-v1`をhardの既定`bao`・`phase2`経路へ正式採用する。expertはAI-GEN3の基準構成を維持する。

| 判断材料 | 確認済み内容と限界 |
| --- | --- |
| 棋力 | P8の100ms・深さ8で512局295勝217敗。P9の500ms・深さ8・毎手Workerで256局151勝105敗。いずれも固定条件の正式判断と検算を通過 |
| 公開経路への組込み | 凍結したモデル・候補探索を保持し、hard限定の共通切替、取得失敗時の復帰、診断、キャッシュ更新、切戻しを用意 |
| 自動確認 | 既知参照との一致、関連Node回帰、Chromium・Firefox・WebKitでの着手・代替実行・取消・キャッシュ・切戻しを確認 |
| 実機 | moto g52j 5G／Android 12／Chrome、約20分。候補の診断表示を確認し、旧ヘッダー表示以外は問題なさそうとのユーザー報告 |
| 残る不確実性 | 端末横断・長時間の使用感、Chromeの版番号、実機での定量遅延、各端末での棋力は未確認 |

hardを採用するための工学上の根拠は得られたと判断する。これは、P8・P9の科学的な適用範囲を広げる判断ではない。P6・P7のHOLD、既存の凍結結果、seed、モデル、閾値は変更しない。実機報告は生の診断ファイルによる独立検算ではなく、報告された範囲の運用確認として採用判断に用いた。

## 採用範囲と表示

`public/ai-release.js`の`PBAI_C015_ENABLED`をtrueにし、hardの既定経路だけに適用する。easy、normal、expert、別profile、独自重み・補正は基準構成を維持する。モデルまたは候補探索を取得できなければ基準AIへ戻る。

ヘッダーはhardで「論理ゲートAI / むずかしい」（英語では「Logic Gate AI / Hard」）、expertなどでは従来のAI-GEN3を表示する。hardのtitleには採用判断IDを用い、診断では実際に使用した候補と復帰の有無を確認できる。応答が基準AIへ復帰した場合は、その着手後の表示も基準AIへ戻す。

この判断IDは新しいAI世代のrelease IDではない。実配備前に世代名を先取りしない命名規則に従い、AI-GEN4への昇格は今回行わない。元の`AI-GEN3-RELEASE-001`とその正式資産5ファイルのhashを保持する。

## 配信と切戻し

正式配信に向け、publicのキャッシュ名をv35へ更新する。mainへ統合した後、mainのpublic一式を公開先へ配信し、配信したcommit、主要ファイルの一致、hardの候補着手、expertの基準AI、表示と更新後キャッシュを確認してから配信状態を更新する。コードの採用・統合を、実サイトの配信完了とは扱わない。

切戻し時は`PBAI_C015_ENABLED`をfalseにし、配信済みとは異なる新しいキャッシュ名へ更新してpublic一式を再配信する。基準AI資産は同じツリーに保持されている。切戻しの実行と結果は追記し、過去の採用判断や棋力結果を上書きしない。

## 文書の時点と品質

[初回採用レビュー](README.md)のHOLDと[組込み時点の記録](INTEGRATION.md)の既定無効は、それぞれの時点の履歴である。現在の採用判断は本書を正本とする。今回の変更は本書、実機記録、入口、組込み案内、中央索引、ルートREADMEの6文書を対象とし、日本語の説明、リンク、固定情報の不変性を確認する。正式名称、ID、コード識別子、hash、コマンド、状態語は許容例外として保持する。

機械可読の正本と配信時の照合用hashは[ADOPTION.json](ADOPTION.json)へ保存した。品質ゲートは`PASS`。6文書の対象箇所で、説明済み例外を除く英語だけの見出し・英語の完全な通常説明文、相対リンク切れ、意図しないコードブロック・固定ID・数値・hash・過去判断変更は0件である。

有効化と表示変更を含むcommit `a8e1d416fe68a666d68e6e1d51a4b093a0665ab0`で、3ブラウザ各9項目が再び成功した（run `34356146841`）。今回は有効化済みpublicをそのまま配信し、hardの候補表示とexpertのAI-GEN3表示も確認した。リリース検証run `34356146842`も成功した。[保存結果](../../../artifacts/pbai-c015-integration/adoption-browser-results.json)を参照する。
