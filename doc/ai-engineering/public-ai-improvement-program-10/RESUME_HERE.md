# PBAI-P10：現在状態と再開手順

## 現在の状態

正式試験は中断し、`TECHNICAL-INVALID / HOLD`で終了した。元の実行セッション2632は回復できず、試験プロセスは残っていない。同じ試験を再起動しない。

開始時刻は2026年9月9日23:52:53 UTC、最後の記録は9月10日00:18:34 UTC。凍結コミットは`6fd1cbee3ff063da9a8025a601510a0bfffbc313`。運用確認16局と低設定10ペア20局が完了し、さらに片側完了1局・途中1局がログに残る。標準・高は未開始。採用や世代昇格はしていない。別のGitHub Actions 2ジョブで実行基盤の受渡し確認は成功したが、新規棋力試験は開始していない。

生記録27ファイル、固定ソース112件の照合、診断検算と事後判断を[中断時証拠](../../../artifacts/pbai-p10/incident/README.md)に保存した。[最終報告](PROGRAM_FINAL_REPORT.md)と[実行基盤の対策](EXECUTION_RECOVERY.md)を次の入口とする。

## 再開時に確認するもの

1. このブランチの最新HEADと、開始markerが示す凍結コミット、[固定計画](PROTOCOL.md)、[SPEC](SPEC.json)を読む。
2. `artifacts/pbai-p10/run/RUN_STARTED.json`と`FINAL_RESULT.json`、実行ログを確認する。開始markerがある場合は新しい正式実行を開始しない。
3. 実行中なら既存プロセスと最後の完了ペアを確認する。停止済みなら、未完了試験を同じseedで再開せず技術的無効として記録する。
4. 完了後は全棋譜再生、Python集計、hash確認、アーカイブ保存、日本語結果報告を行う。main統合はユーザー指示待ち。

## 過去の実行コマンド（再実行禁止）

以下は過去の実行記録であり、現在は実行しない。開始markerがあるため監視器も再起動を拒否する。

```sh
python tools/engineering/supervise-pbai-p10.py
```

監視器の自己試験は対局seedを消費しない。正式試験開始後のソース変更・再起動を避け、結果を理由に条件を修正しない。
