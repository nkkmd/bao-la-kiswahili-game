# 局面相転移点研究 — 判断台帳

更新日: 2026-07-30

## 状態

- `提案`: 未検証
- `暫定採用`: 現在の分析で使用するが、確認的検証前
- `採用`: 以後の研究設計に固定
- `撤回`: 根拠により使用を中止
- `保留`: 判断材料不足

## 判断一覧

| ID | 判断 | 状態 | 根拠・備考 |
|---|---|---|---|
| D-001 | `gameId + ply` を観測の一意キーとする | 採用 | Phase 0再現性要件 |
| D-002 | `observations.jsonl` を一次正本とする | 採用 | 派生表は再生成可能とする |
| D-003 | 非baselineゲームのランダム開局観測を候補検出から除外する | 採用 | 開局方策の人工的変動を候補に混入させない |
| D-004 | 最終観測を候補検出から除外する | 採用 | 終局判定直前の縮退を主候補から分離する |
| D-005 | 隣接候補plyを区間クラスタへ統合する | 採用 | 同一変化を複数候補として数えない |
| D-006 | forcingイベント距離と構造イベント距離を分離する | 採用 | 循環的説明を避ける |
| D-007 | forcingを候補成立の独立特徴群として数える | 撤回 | forcing除外で95区間から45区間へ減少し、過剰検出が確認された |
| D-008 | forcingは候補成立に使わず、補助情報として保持する | 暫定採用 | 非forcing特徴2群以上を要求する |
| D-009 | 主閾値を `signal=2.0 / persistence=0.75` とする | 暫定採用 | パイロット内のバランス設定。確認用コーパス前には固定手続きが必要 |
| D-010 | strict閾値を `2.5 / 1.0` とする | 暫定採用 | 強い候補の監査用。正式検出器の主設定ではない |
| D-011 | 区間対応は代表ply一致で判定する | 撤回 | 同一区間でも代表plyが変化する |
| D-012 | 区間対応は同一gameIdかつ区間重複で判定する | 採用 | A/B/C/X分類に使用 |
| D-013 | 候補をA/B/C/Xへ分類する | 暫定採用 | A: forcing切替非同時、B: forcing同時・非forcing支持、C: forcing依存、X: excluded側のみ |
| D-014 | `nearestForcingDistance > 0` をforcing独立と解釈する | 撤回 | 主要6候補はすべて `forcedCapture=true` の継続区間内だった |
| D-015 | Aを「forcing切替非同時候補」と呼ぶ | 採用 | forcing状態そのものからの独立を意味しない |
| D-016 | `pliesRemaining < 5` を終局近傍として主候補から分離する | 暫定採用 | A 13アーキタイプ中7件が終局まで1〜3plyだった |
| D-017 | stateHashと変化シグネチャを分けて集計する | 採用 | 固有局面数と固有変化型を区別する |
| D-018 | 同一変化シグネチャをアーキタイプとして集約する | 暫定採用 | 13 Aアーキタイプを抽出。署名設計は今後拡張予定 |
| D-019 | 主要6候補を正式な戦略的相転移と認定する | 保留 | 全候補が強制捕獲レジーム内。候補手の質的分析と新規seed追試が必要 |
| D-020 | `2e79188a987a` と `7360876ad5c7` をmtaji前兆候補として扱う | 暫定採用 | 候補後8ply以内に `namua → mtaji` が発生 |
| D-021 | 残る主要4件を捕獲分岐爆発候補として扱う | 暫定採用 | 捕獲手数が急増し、強制捕獲系列が継続 |
| D-022 | 次の分析単位をplyではなく強制捕獲レジームとする | 採用 | forcing切替距離だけでは系列内部の構造を表現できない |
| D-023 | Colab結果だけで正式結論を出さない | 採用 | 固定ローカル環境での再現を正式要件とする |

## 今後固定が必要な判断

- 主検出閾値
- 持続期間
- 終局近傍除外幅
- 強制捕獲レジームの最低長
- 捕獲分岐急変の定義
- アーキタイプ署名に含める特徴量
- 新規seed確認実験のサンプル数
