# Stage D machine definition / Bao research vocabulary completion

Date: 2026-08-07
Status: completed
Branch: `research/forced-capture-regime-analysis`

## Completion

Study 1 Stage D is complete with the dedicated vocabulary source:

- `doc/phase-transition/STUDY_1_VOCABULARY.md`

The document fixes the mapping between implemented machine quantities/classifiers and bounded human-facing research terms without changing any historical threshold, classifier order, experiment decision, or archive.

## Key governance decisions

1. `capture-branch-expansion` retains the existing classifier order and fixed defaults.
2. Empirical forced-capture-regime membership is **not** added post hoc as a new classifier requirement.
3. `captureMoveCount` is called a legal capture-option count / capture-move branching proxy, not a full search-tree branching factor.
4. `sustained-forcing window` is fixed as an interpretive Stage B structural term, not a newly thresholded classifier.
5. `trajectory-ply` remains a secondary structural sensitivity unit and does not replace paired game-level formal endpoints.
6. `search-profile dependence` is explicitly condition-bounded and does not imply a formally confirmed general search-profile × depth interaction.
7. Preferred final language is `capture-branch-expansion strategic-transition phenotype` or `strong phase-transition candidate with bounded recognition scope` rather than an unqualified universal Bao phase transition.

## Fixed formal decisions

Unchanged:

- E-010 `not-confirmed`;
- E-011 `inconclusive`;
- E-017 `not-confirmed`;
- E-018/H16 `confirmed` only at fixed depth2 condition;
- E-019/H17 global `not-confirmed`;
- E-020/H18 `confirmed` only at fixed depth3 condition.

## Next stage

Proceed to Stage E — final Study 1 integration.

No new data generation or formal experiment is required for Stage E.