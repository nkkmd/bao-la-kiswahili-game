# E-019 authorization-state validation checkpoint

更新日: 2026-08-03  
Experiment: `E-019`  
Hypothesis: `H17`  
analysisVersion: `17-search-profile-generalization`  
Status: Formal authorized / authorization-state CI validated / awaiting fixed-local execution lock / formal corpus not generated

## 1. Purpose

E-019固有のformal開始承認を受領し、execution policyを

```text
status: approved-awaiting-local-lock
formalExecutionAllowed: true
formalCorpusGenerated: false
githubActionsFormalRunAllowed: false
```

へ遷移した。

このauthorization stateで、formal guardとnon-formal fixture infrastructureが壊れていないことをGitHub Actionsで再検証した。

## 2. Validation

Workflow:

```text
Phase Transition Search Profile Generalization
```

Actions run:

```text
30779314186
```

validated head:

```text
0ca82e95334e5e5943999a54ba6946fb64b6e0de
```

Result:

```text
success
```

Successful stages:

- preregistration/evaluator/IUT-Holm tests
- authorization-aware formal guard tests
- six-condition non-formal fixture generation
- three-stratum paired fixture integrity
- candidate/control analysis for all six conditions
- paired endpoint construction
- fixture IUT/Holm output-contract evaluation
- preregistered structural secondary
- artifact upload

Artifact:

- ID: `8843108847`
- name: `phase-transition-search-profile-generalization-fixture`
- digest: `sha256:d3d32c5a4a5b857dd6da86a99cf50cdbcdd1f569a6c2cf085e6dd9fb7c2d1f4c`
- size: `221411` bytes

## 3. Formal generation remains local-only

このCIはformal execution authorization後に実行されたが、formal corpusを生成していない。

- fixture seed: formal seed block外
- formal corpus generated: false
- GitHub Actions formal generation: prohibited
- execution lock: not generated by CI

したがってauthorizationはGitHub Actionsでのformal生成を許可しない。

## 4. Scientific contract unchanged

authorizationおよびstate validationによって以下を変更していない。

- D1/D3/V2 strata
- paired sample sizes 6500 / 4500 / 2000
- formal master seed block `20268001–20274500`
- candidate/regime thresholds
- primary population `pliesRemaining >= 9`
- paired game-level binary endpoint
- exact two-sided McNemar per stratum
- component alpha 0.05
- minimum discordant pairs 20
- direction phase2-only > legacy-only
- global IUT rule
- Holm standalone inference
- structural secondary boundary

E-010 `not-confirmed`、E-011 `inconclusive`、E-017 `not-confirmed`、E-018 `confirmed`も変更しない。

## 5. Next step

固定ローカルrepositoryを最新branch headへ同期し、venv/runtime/branch/clean worktree/git-ignoreを再確認する。

その後、E-019専用execution lockを生成する。

```bash
node tools/experiments/prepare-phase-transition-search-profile-generalization-execution.js
```

lockの`errors: []`、`status: prepared-approved`、`approval.approved: true`、source commit、preregistration/policy hashおよび固定条件を監査するまでformal corpusを生成しない。
