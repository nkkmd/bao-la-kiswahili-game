# FDEGHV-STUDY1 — Study 1 Protocol

更新日: 2026-09-04

## 1. Study identity

```text
Program = Research Generation 3 / G3-11
Study ID = FDEGHV-STUDY1
Stage 0 = FDEGHV-S0-TECHNICAL-2026-09-04-v1
Stage 1 = FDEGHV-S1-FORMAL-HOLDOUT-2026-09-04-v1
Reviewed main anchor = e537199a959c0808cbef6cf8aaeb1caab91e3702
```

## 2. Formal question

Can the previously sealed standard initial Bao RAW root be completely and independently enumerated through exact depth 10, and do four prospectively frozen deeper exact geometry continuation targets hold in that complete domain without symmetry reduction, canonicalization collapse, G2-12 estimator input, subset rescue, or depth-11 extension?

## 3. Evidence classes

```text
Stage 0 = TECHNICAL-FIXTURE / HISTORICAL-EXACT-REFERENCE ONLY
Stage 1 = FRESH-DEEPER-EXACT-HOLDOUT
```

Stage 0 may use synthetic controls and already public/historical depth <=9 exact prefixes. It must not generate or read any depth-10 scientific outcome.

Stage 1 is the only protected scientific evidence stage. Exactly one first-fresh scientific execution is permitted after a separate machine-readable authorization and durable lease.

## 4. Representation contract

Authoritative state identity:

```text
pits,reserve,houseOwned,player,phase,winner,pending
```

Excluded identity fields:

```text
turn,reason
```

Move identity:

```text
type,phase,row,index,direction,side,houseChoice,houseTwo
```

Boundary:

```text
mode = RAW-ONLY
validated transform set = []
symmetry reduction = false
canonicalization-based deduplication = false
seat-swap equivalence = false
left/right quotient = false
```

## 5. Exact domain contract

```text
root = public/engine.js initialState() reconstructed fresh in the authorized run
required root RAW key = 2c13e69c51d58e2605bf6018ac848d99685aa4d4fe78c0af9f8e0fc07e1d3fd6
target depth = 10
complete reachable layers = 0..10
complete parent expansion layers = 0..9
complete forward closure to terminal = not required
```

No depth-11 successor generation, depth-11 count, depth-11 partial probe, or outcome-dependent extension is authorized.

## 6. Formal target family

### E0 — exact domain completion

The primary formal result is whether the complete frozen depth-10 domain can be produced and independently reproduced under the frozen resource contract.

### H1 — exact-depth novelty continuation

```text
newRawStateCount[10] == uniqueRawStateCount[10]
```

### H2 — layer tree/RAW divergence continuation

```text
treeNodeOccurrences[10] > uniqueRawStateCount[10]
```

### H3 — cumulative tree/RAW inflation continuation

Compare exactly as integers:

```text
treeThrough10 * rawThrough9 > treeThrough9 * rawThrough10
```

No floating-point tolerance is used for the decision.

### H4 — transposition persistence

```text
duplicateArrivalCount[10] > 0
AND statesWithMultiplePredecessors[10] > 0
```

## 7. Construct exclusions

G3-11 does not re-decide:

- G3-04 paired Namua/Mtaji C1 or C6 decisions;
- G3-07 search-condition association decisions;
- CRCLGR continuous-representation eligibility;
- G3-10 trajectory chronology C1–C5;
- any upstream technical-invalid, non-estimable, inconclusive, confirmed, or not-confirmed decision.

The same metric may appear as a descriptive exact quantity without implying revalidation of the original inferential construct.

## 8. Formal resource ceiling

Per production or independent full enumeration:

```text
max cumulative distinct RAW states = 2,000,000
max depth-labelled edges = 12,000,000
max parent expansions = 2,000,000
max move evaluations = 12,000,000
max cumulative tree node occurrences = 50,000,000,000
max resident set bytes = 6,442,450,944
max wall clock seconds = 5,400
max uncompressed scientific artifact bytes = 1,073,741,824
```

These are ceilings, not expected values. G2-12 output is not used to tune them.

A cap crossing after first fresh access is a formal fail-closed event. No same-Study cap increase is allowed.

## 9. Mandatory hardening

The implementation must adopt the prospective post-G2-05 hardening requirements:

1. every claimed-complete layer must be independently re-enumerated;
2. final ambient/resource state must be rechecked after final scientific materialization and before exact-complete classification;
3. Stage 0 must test success and forced failure paths;
4. G3-11 uses new Study/Stage/source/authorization identities;
5. later repository audits are read-only and cannot rerun the protected evidence.

## 10. Independent verification

The independent verifier may import the shared rule engine but must not import the production enumerator, production serializer, or production scientific target evaluator.

It must independently recompute:

- layer RAW state sets 0..10;
- cumulative RAW state set;
- complete exact source-move-successor relation for parent layers 0..9;
- tree occurrence propagation;
- branching distribution for parent layers 0..9;
- duplicate-arrival, predecessor, and multi-parent accounting;
- per-layer/cumulative hashes;
- H1–H4 target decisions.

Exact completion requires full independent depth-10 re-enumeration and exact agreement.

## 11. Decision rule

### Domain-level result

```text
complete + final resource PASS + production integrity PASS + independent exact PASS
  => EXACT-WITHIN-FROZEN-DEPTH-10-DOMAIN

resource/admin ceiling prevents completion while integrity classification remains valid
  => NON-ESTIMABLE

source/integrity/verification/serialization/final-classification defect
  => TECHNICAL-INVALID
```

### Target-level result

Only if the domain-level result is exact:

```text
H1..H4 condition true  => DEEPER-CONFIRMED
H1..H4 condition false => COUNTEREXAMPLE-BOUNDARY
```

If the domain is not exact, H1–H4 are `NON-ESTIMABLE` and partial evidence cannot be promoted.

## 12. No-rescue / stopping rule

After first depth-10 protected scientific access:

- one authorized execution only;
- no rerun under FDEGHV-STUDY1;
- no ceiling increase;
- no target change;
- no subset-only formal claim;
- no partial-layer promotion;
- no root change;
- no symmetry/canonicalization rescue;
- no G2-12 prediction use;
- no depth 11.

Any future depth 11+ work is a separate prospective Study.

## 13. Repository workflow

Required order:

1. current main identity recorded;
2. Study/Stage/spec/target/resource freeze;
3. production/independent source binding;
4. Stage 0 authorization and technical validation;
5. documentation synchronization defect corrected;
6. separate Stage 1 authorization artifact;
7. durable pre-computation lease;
8. exactly one protected Stage 1 execution;
9. canonical Actions artifact upload;
10. independent full verification;
11. compact canonical exact-byte repository mirror;
12. formal decision and closure docs;
13. cross-document consistency audit;
14. stop before `main` integration.
