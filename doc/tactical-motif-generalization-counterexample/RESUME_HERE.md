# RESUME HERE

## 現在地

G2-09 `TMGC-STUDY1`は開始済み。baselineは`bc1263b7076f0a3794da5fd0d4e07821b23e1db6`、branchは`research/g2-09-tactical-motif-generalization-counterexample`。

Stage 0 technical-onlyがauthorizedで、Stage 1 / Stage 2はnot authorized。科学的seedは一切消費していない。

## Reserved seeds

- Stage 1 `29110001..29114096`: `UNCONSUMED`
- Stage 2 `29210001..29218192`: `UNCONSUMED`

## 次に行うこと

1. `preregistration/UPSTREAM_C03_FROZEN_REFERENCE.json`とResearch Generation 1のcandidate/spec/authorizationを照合する。
2. `run-tactical-motif-stage2-formal.js`、`verify-tactical-motif-stage2-formal.js`、`evaluate-tactical-motif-stage2-formal.js`と関連libの依存関係を監査する。
3. C03 exact classification、RAW identity、legal move、successor、D3 top-set、paired diagnostic semanticsが独立再構築可能か確認する。
4. phase-transport候補のsemantic eligibilityをoutcomeなしで判定する。
5. fresh source-generation firewall、source-policy balance、opening-prefix diversity、resource/artifact preflightを設計する。
6. mandatory Stage 0 checks完了後にformal Stage 0 dispositionを記録する。

Stage 0 PASS前にStage 1 scientific generationを開始しない。
