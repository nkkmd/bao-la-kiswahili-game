# STAGE_0_TECHNICAL_PLAN — Symmetry / Isomorphic Positions Study 1

Stage 0 is technical. It must not produce or inspect the Study 1 formal reachable-corpus candidate pass/fail outcome.

## 1. Synthetic/control fixtures

Use manually specified or standard-initial technical fixtures to test:

- state transform totality;
- involution/inverse;
- exact move transform inverse;
- preservation of diagnostic fields;
- identity positive control;
- intentionally incomplete LR/no-direction negative control;
- active-house applicability rejection for T02/T03;
- terminal winner player-permutation mapping.

Candidate semantics are already frozen before these tests. A failed scientific transform fixture cannot be repaired by editing its semantics; a pure coding defect may be corrected only under the pre-generation correction rule.

## 2. Fresh technical seed block

```text
22900001..22900256
```

This block is for feasibility only and must not become Stage 1 scientific evidence.

## 3. Technical graph benchmark

Generate deterministic historical trajectories from standard initial state. Collect phase-stratified raw source states without evaluating symmetry success.

Benchmark candidate local expansion depths such as `1,2,3` using only:

```text
root count
phase balance
unique raw state count
exact edge count
maximum branching
runtime
memory
relay-limit / incomplete-transition count
maximum observed relay work when available
```

Do not record candidate-specific commutation mismatch rates in this benchmark.

## 4. Stage 1 size selection

Choose source-root count and local depth using a deterministic rule based only on technical feasibility. The final rule and measured quantities must be recorded before the formal seed block is generated.

## 5. Formal seed reserve

A disjoint formal block begins at:

```text
22910001
```

The exact number of consumed seeds is not frozen until Stage 0 technical sizing is complete. No formal candidate outcome may be generated before that freeze.

## 6. Required Stage 0 outputs

- transform implementation source hash
- candidate JSON blob/hash
- synthetic/control audit
- technical graph benchmark JSON
- selected formal graph size/depth rationale
- Stage 1 spec source bindings
- explicit authorization that no formal outcome existed before freeze
