# STAGE_1_FORMAL_RUNBOOK — Symmetry / Isomorphic Positions Study 1

Status: **DRAFT CONTRACT — NOT AUTHORIZED**

This runbook cannot authorize formal execution until Stage 0 technical sizing is complete and a machine-readable Stage 1 domain/spec is frozen.

## Planned gates per candidate/scope

```text
G1 transform totality
G2 transformed-state validity / applicability
G3 inverse / bijection
G4 exact legal-move-set bijection
G5 exact transition commutation
G6 terminal equivalence
G7 winner equivariance under player permutation
G8 reachability-status contract
G9 prior-oracle value preservation
G10 prior-oracle DTF preservation
G11 prior-oracle optimal-move-set preservation
G12 production / independent verifier equality
```

`N/A` is allowed only with an explicit prospective reason.

## Prior exact-oracle anchor

For every applicable scientific transform:

1. read canonical raw oracle without modification;
2. transform all 8 raw states and 7 raw edges;
3. independently generate legal edges from transformed states;
4. require exact state/edge bijection and commutation;
5. independently retrograde solve transformed graph;
6. compare value, winner under `π`, DTF, optimal moves under `Φ`, and SCC structure.

## Fresh bounded-local graphs

Formal roots will be historically reachable and have replayable initial-state witnesses. The root selection rule and expansion depth will be frozen after Stage 0 using no symmetry outcome.

Every exact move from every expanded node must be checked. `99.x%` cannot validate a candidate.

## Administrative failure

If a move successor is not normatively determined because only an implementation/resource guard was reached, do not count it as a symmetry mismatch. Apply the frozen technical-failure/non-estimability rule.

## Decision

- all applicable gates PASS with zero mismatches: `VALIDATED-BOUNDED-ISOMORPHISM`
- at least one exact semantic mismatch: `NOT-VALIDATED`
- required complete verification unavailable for technical/resource reasons: `NON-ESTIMABLE`

## Conditional downstream work

Only after validation may this Study construct a canonicalization contract or test composition/group structure. Those are derived results and cannot alter primary candidate decisions.
