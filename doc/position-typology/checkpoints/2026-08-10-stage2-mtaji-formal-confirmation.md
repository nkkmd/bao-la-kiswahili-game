# Checkpoint — Stage 2 Mtaji Formal Confirmation

Date: 2026-08-10

## Formal decision

Preregistration:

```text
PTYP-S2-MTAJI-CONFIRM-2026-08-10-v1
```

Candidate definition hash:

```text
7a276a8c795efddaf9b9555e0fbb7fdc1e56563d30ebcb352b42b3f53ba0e75d
```

Formal result hash:

```text
26b429e75f7a8ffd5681f3ba1f7b1915b8ad6f0470b1d3155f07381ffd6c5347
```

Decision:

```text
confirmed
```

## Gate record

Technical gates:

- full replay verification: passed
- contributing games: 180 >= 144
- capped mtaji rows: 2256 >= 1500

Primary gates:

- G1 minority frozen type fraction: 0.4481 >= 0.20 — passed
- G2 frozen-label silhouette: 0.1964 >= 0.12 — passed
- G3 BIC discreteness: BIC2=10722.08; improvement over BIC1=890.28; BIC3-BIC2=13.25 — passed
- G4 de-novo ARI: K-means=0.9167, GMM=0.9184, Ward=0.9031; median=0.9167 — passed
- G5 80%-game subsample ARI p10: 0.8931 >= 0.60 — passed

No post-hoc rescue was used.

## Frozen interpretation

The confirmed claim is:

> A two-type actor/opponent-invariant structural morphology in Bao mtaji, represented by the frozen 40-dimensional classifier, independently replicates under the preregistered Stage 2 held-out design.

Canonical study IDs:

```text
MTAJI-M1
MTAJI-M2
```

Current descriptive aliases:

```text
MTAJI-M1 = capture-engaged / relatively balanced morphology
MTAJI-M2 = capture-sparse / relatively asymmetric morphology
```

The IDs are now confirmed study types under the fixed classifier/population boundary. The descriptive English aliases remain wording-level provisional labels pending ontology naming review.

## Boundaries that remain fixed

Do not reinterpret this result as:

- a universal ontology for all Bao states,
- a namua two-type result,
- a playing-style classification,
- a win/loss or advantage classification,
- an evaluator/search implementation classification,
- evidence that revises any closed Study 1 formal decision.

The actor-oriented mtaji k=2 remains a continuous relational-polarity coordinate, not two intrinsic types.

Namua remains:

```text
no discrete position-type candidate promoted
```

## Next authorized work

Separate follow-up streams may proceed without altering this formal decision:

1. ontology/naming review for MTAJI-M1/M2,
2. namua continuous-gradient analysis,
3. playing-style analysis based on trajectories through confirmed mtaji types and other trajectory-level coordinates,
4. later cross-study relation analysis with closed Study 1.

The Stage 2 confirmation corpus must not be reused to redefine/refit the confirmed classifier and then presented as the same confirmation.
