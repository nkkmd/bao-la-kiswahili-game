# Checkpoint — Mtaji Ontology Naming / Namua Gradient Start

Date: 2026-08-10

## Fixed mtaji result

Stage 2 independent confirmation remains:

```text
formal decision = confirmed
candidateDefinitionHash = 7a276a8c795efddaf9b9555e0fbb7fdc1e56563d30ebcb352b42b3f53ba0e75d
```

No classifier, population, scaler, centroid, or canonical-ID change is made here.

## Final human-readable ontology names

Canonical machine/research IDs remain:

```text
MTAJI-M1
MTAJI-M2
```

Human-readable names are fixed as:

```text
MTAJI-M1 = Capture-Engaged Low-Contrast Morphology
MTAJI-M2 = Capture-Sparse High-Contrast Morphology
```

The naming change replaces only the earlier discovery wording `relatively balanced / relatively asymmetric`.

`Low-Contrast / High-Contrast` explicitly refers to actor/opponent absolute structural differences in the frozen role-invariant representation. It does not refer to win probability, player strength, or advantage.

## Namua decision retained

Prior negative exploratory conclusion remains unchanged:

```text
namua k=2 = not promoted
namua k=4 = not promoted
no discrete namua position-type candidate
```

The next analysis does not rescue those clusters.

## New authorized exploratory stream

Stage 3 namua continuous-gradient audit is authorized using only the original Stage 1 discovery pilot.

Tool:

```text
tools/experiments/analyze-position-typology-stage3-namua-gradients.py
```

Runbook:

```text
doc/position-typology/STAGE_3_NAMUA_GRADIENT_RUNBOOK.md
```

Primary objects:

- reserve-depletion coordinate,
- capture-activity coordinate,
- structural-contrast coordinate,
- role-invariant PCA geometry,
- density continuity/multimodality diagnostics,
- trajectory monotonicity,
- game-level subspace stability.

Raw ply remains descriptive only and is not an input feature.

## Boundaries preserved

- Study 1 formal decisions unchanged.
- Stage 2 mtaji confirmation unchanged.
- No Stage 2 held-out rows are reused for initial namua gradient discovery.
- No future namua confirmation corpus is touched.
- No playing-style labels are created yet.
- AI implementation labels are not playing styles.
