# Checkpoint — Stage 3 Namua Continuous Result / Stage 4 Playing-Style Start

Date: 2026-08-10

## Fixed Stage 3 decision

Stage 3 exploratory audit hash:

```text
099f376fc3ab421165d9f04cb0544a78ea3170409e1a237dd7cb31f0a6cf9c0a
```

Decision:

```text
namua discrete k=2 / k=4 remain rejected as position-type candidates
namua is represented downstream by continuous coordinates
```

Downstream namua coordinates:

```text
N-PROG = deterministic reserve-depletion progress context
N-ACT  = continuous capture-activity coordinate
N-CON  = continuous structural-contrast coordinate
```

`N-PROG` is not an independent morphology axis and is excluded from the primary playing-style feature vector.

No new namua cluster search is authorized by this checkpoint.

## Mtaji boundary unchanged

Formal Stage 2 decision remains:

```text
confirmed
```

Frozen classifier hash:

```text
7a276a8c795efddaf9b9555e0fbb7fdc1e56563d30ebcb352b42b3f53ba0e75d
```

Confirmed human-readable ontology:

```text
MTAJI-M1 = Capture-Engaged Low-Contrast Morphology
MTAJI-M2 = Capture-Sparse High-Contrast Morphology
```

No refit or redefinition is authorized.

## Stage 4 authorization

Playing-style discovery may now begin under these constraints:

- unit = one game trajectory
- Stage 1 discovery games only
- full-phase games primary
- AI condition labels metadata only
- no condition label may be called a playing style
- confirmed mtaji occupancy/dwell/transition descriptors may be used
- namua N-ACT/N-CON trajectory summaries may be used
- same-pilot robustness is exploratory only
- no final style count or style name before geometry/interpretability audit
- no future style-confirmation seeds touched

Implementation:

```text
tools/experiments/analyze-position-typology-stage4-playing-style.py
```

Runbook:

```text
doc/position-typology/STAGE_4_PLAYING_STYLE_RUNBOOK.md
```

## Preserved global boundaries

- position type != playing style
- actor-oriented mtaji polarity remains a continuous relational coordinate, not an intrinsic type
- Study 1 formal decisions remain unchanged
- Stage 1 negative/null namua clustering results are not rescued post hoc
- Stage 2 mtaji confirmation is not retroactively altered by Stage 3/4
