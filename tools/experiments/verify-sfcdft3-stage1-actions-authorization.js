#!/usr/bin/env node
"use strict";
const fs=require('node:fs'),path=require('node:path'),crypto=require('node:crypto');const ROOT=path.resolve(process.cwd());const SPEC_PATH='doc/structural-forcing-corridor-tree-raw-transfer/prereg/STUDY_3_STAGE_1_COMPATIBILITY_SPEC.json',S0_PATH='doc/structural-forcing-corridor-tree-raw-transfer/results/stage-0-study3/STUDY_3_STAGE_0_TECHNICAL_RESULT.json',BINDING_PATH='doc/structural-forcing-corridor-tree-raw-transfer/authorizations/STUDY_3_STAGE_1_PRE_EXECUTION_BINDING.json',AUTH_PATH='doc/structural-forcing-corridor-tree-raw-transfer/authorizations/STUDY_3_STAGE_1_EXECUTION_AUTHORIZATION.json';function need(x,m){if(!x)throw new Error(m);}function read(rel){return JSON.parse(fs.readFileSync(path.join(ROOT,rel),'utf8'));}function blob(rel){const b=fs.readFileSync(path.join(ROOT,rel)),h=Buffer.from(`blob ${b.length}\0`);return crypto.createHash('sha1').update(h).update(b).digest('hex');}
function main(){need(fs.existsSync(path.join(ROOT,AUTH_PATH)),'Study3 final Stage1 authorization missing');const spec=read(SPEC_PATH),s0=read(S0_PATH),binding=read(BINDING_PATH),auth=read(AUTH_PATH);need(spec.studyId==='SFCDFT-STUDY3'&&spec.stageId==='SFCDFT3-S1-COMPATIBILITY-2026-09-19-v1','spec identity mismatch');need(s0.studyId==='SFCDFT-STUDY3'&&s0.decision==='STAGE0-PASS'&&s0.deterministicCoreSha256===spec.prerequisiteStage0.deterministicCoreSha256,'Stage0 prerequisite mismatch');need(binding.studyId===spec.studyId&&binding.stageId===spec.stageId&&binding.status==='FROZEN-PRE-EXECUTION','binding mismatch');need(auth.studyId===spec.studyId&&auth.stageId===spec.stageId&&auth.decision==='SFCDFT3-STAGE1-EXECUTION-AUTHORIZED','authorization mismatch');need(/^[0-9a-f]{40}$/.test(auth.authorizedParentHead||''),'authorized parent invalid');if(process.env.GITHUB_ACTIONS==='true'){const event=JSON.parse(fs.readFileSync(process.env.GITHUB_EVENT_PATH,'utf8'));need(event.before===auth.authorizedParentHead,`authorized parent mismatch ${event.before}`);need(process.env.GITHUB_RUN_ATTEMPT==='1','Stage1 rerun forbidden');}
 need(auth.freshScientificSeedAccessAuthorized===true,'fresh access not authorized');need(auth.stage2ScientificSeedAccessAuthorized===false,'Stage2 must remain sealed');need(auth.preExecutionBindingGitBlobSha===blob(BINDING_PATH),'binding blob mismatch');for(const[rel,expected]of Object.entries(binding.boundGitBlobs)){need(fs.existsSync(path.join(ROOT,rel)),`bound file missing ${rel}`);need(blob(rel)===expected,`bound blob mismatch ${rel}`);}const p=spec.freshSeedSlots.primary,r=spec.freshSeedSlots.pairedReserve;need(p.seedStart===40511001&&p.seedEnd===40511384&&p.count===384,'primary namespace mismatch');need(r.seedStart===41511001&&r.seedEnd===41511384&&r.count===384,'reserve namespace mismatch');need(spec.freshSeedSlots.maxFreshSeedReads===400&&spec.freshSeedSlots.maxInfrastructureReplacements===16,'spec access ceiling mismatch');need(auth.maxFreshSeedReads===400&&auth.maxInfrastructureReplacements===16,'auth access ceiling mismatch');need(auth.scientificEffectMagnitudeAuthorized===false&&auth.effectDirectionAuthorized===false&&auth.pValueAuthorized===false&&auth.generalizationDecisionAuthorized===false&&auth.counterexampleDecisionAuthorized===false,'compatibility-only boundary mismatch');need(auth.fullFreshWorkflowRerunAuthorized===false&&auth.sameEvidenceRepairRerunAuthorized===false&&auth.manualWorkflowDispatchAuthorized===false&&auth.runAttemptGreaterThanOneAuthorized===false,'no-rerun boundary mismatch');need(auth.requiredPreflight?.conclusion==='success'&&Number.isInteger(auth.requiredPreflight.runId)&&Number.isInteger(auth.requiredPreflight.artifactId),'successful preflight binding missing');need(auth.study1ReplayAuthorized===false&&auth.study2ReplayAuthorized===false&&auth.mainIntegrationAuthorized===false,'protected boundary mismatch');process.stdout.write(JSON.stringify({authorized:true,studyId:spec.studyId,stageId:spec.stageId,authorizedParentHead:auth.authorizedParentHead,boundFileCount:Object.keys(binding.boundGitBlobs).length,preflightRunId:auth.requiredPreflight.runId})+'\n');}
main();
