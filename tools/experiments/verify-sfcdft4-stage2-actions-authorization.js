#!/usr/bin/env node
"use strict";
const fs=require('node:fs'),path=require('node:path'),crypto=require('node:crypto');
const ROOT=path.resolve(process.cwd());
const SPEC_PATH='doc/structural-forcing-corridor-tree-raw-transfer/prereg/STUDY_4_STAGE_2_FORMAL_HELDOUT_SPEC.json';
const S1_PATH='doc/structural-forcing-corridor-tree-raw-transfer/results/stage-1-study4/STUDY_4_STAGE_1_COMPATIBILITY_RESULT.json';
const BINDING_PATH='doc/structural-forcing-corridor-tree-raw-transfer/authorizations/STUDY_4_STAGE_2_PRE_EXECUTION_BINDING.json';
const AUTH_PATH='doc/structural-forcing-corridor-tree-raw-transfer/authorizations/STUDY_4_STAGE_2_EXECUTION_AUTHORIZATION.json';
function need(x,m){if(!x)throw new Error(m);}function read(rel){return JSON.parse(fs.readFileSync(path.join(ROOT,rel),'utf8'));}function blob(rel){const b=fs.readFileSync(path.join(ROOT,rel)),h=Buffer.from(`blob ${b.length}\0`);return crypto.createHash('sha1').update(h).update(b).digest('hex');}
function main(){need(fs.existsSync(path.join(ROOT,AUTH_PATH)),'Study4 final Stage2 authorization missing');const spec=read(SPEC_PATH),s1=read(S1_PATH),binding=read(BINDING_PATH),auth=read(AUTH_PATH);need(spec.studyId==='SFCDFT-STUDY4'&&spec.stageId==='SFCDFT4-S2-FORMAL-HELDOUT-2026-09-19-v1','spec identity mismatch');need(s1.studyId==='SFCDFT-STUDY4'&&s1.decision==='FORMAL-PREPARATION-ELIGIBLE'&&s1.deterministicCoreSha256===spec.prerequisiteStage1.deterministicCoreSha256,'Stage1 prerequisite mismatch');need(binding.studyId===spec.studyId&&binding.stageId===spec.stageId&&binding.status==='FROZEN-PRE-EXECUTION','binding mismatch');need(auth.studyId===spec.studyId&&auth.stageId===spec.stageId&&auth.decision==='SFCDFT4-STAGE2-EXECUTION-AUTHORIZED','authorization mismatch');need(/^[0-9a-f]{40}$/.test(auth.authorizedParentHead||''),'authorized parent invalid');if(process.env.GITHUB_ACTIONS==='true'){const event=JSON.parse(fs.readFileSync(process.env.GITHUB_EVENT_PATH,'utf8'));need(event.before===auth.authorizedParentHead,`authorized parent mismatch ${event.before}`);need(process.env.GITHUB_RUN_ATTEMPT==='1','Stage2 rerun forbidden');}need(auth.freshScientificSeedAccessAuthorized===true,'fresh access not authorized');need(auth.preExecutionBindingGitBlobSha===blob(BINDING_PATH),'binding blob mismatch');for(const[rel,expected]of Object.entries(binding.boundGitBlobs)){need(fs.existsSync(path.join(ROOT,rel)),`bound file missing ${rel}`);need(blob(rel)===expected,`bound blob mismatch ${rel}`);}const p=spec.freshSeedSlots.primary,r=spec.freshSeedSlots.pairedReserve;need(p.seedStart===40621001&&p.seedEnd===40621768&&p.count===768,'primary namespace mismatch');need(r.seedStart===41621001&&r.seedEnd===41621768&&r.count===768,'reserve namespace mismatch');need(spec.freshSeedSlots.maxFreshSeedReads===792&&spec.freshSeedSlots.maxInfrastructureReplacements===24,'spec access ceiling mismatch');need(auth.maxFreshSeedReads===792&&auth.maxInfrastructureReplacements===24,'auth access ceiling mismatch');need(auth.formalInferenceAuthorized===true&&auth.effectDirectionAuthorized===true&&auth.pValueAuthorized===true&&auth.generalizationDecisionAuthorized===true&&auth.counterexampleDecisionAuthorized===true,'formal output authorization mismatch');need(auth.fullFreshWorkflowRerunAuthorized===false&&auth.sameEvidenceRepairRerunAuthorized===false&&auth.manualWorkflowDispatchAuthorized===false&&auth.runAttemptGreaterThanOneAuthorized===false,'no-rerun boundary mismatch');need(auth.requiredPreflight?.conclusion==='success'&&Number.isInteger(auth.requiredPreflight.runId)&&Number.isInteger(auth.requiredPreflight.artifactId),'successful preflight binding missing');need(auth.study3Stage2RepairOrRerunAuthorized===false&&auth.study4Stage1ReplayAuthorized===false&&auth.g4_10Depth11AccessAuthorized===false&&auth.mainIntegrationAuthorized===false&&auth.publicAiChangeAuthorized===false,'protected boundary mismatch');process.stdout.write(JSON.stringify({authorized:true,studyId:spec.studyId,stageId:spec.stageId,authorizedParentHead:auth.authorizedParentHead,boundFileCount:Object.keys(binding.boundGitBlobs).length,preflightRunId:auth.requiredPreflight.runId})+'\n');}
main();
