"use strict";
const fs=require("node:fs");const path=require("node:path");const crypto=require("node:crypto");const E=require("../../public/engine.js");
const F=require("./lib/rcpr-independent.js");const I=require("./lib/rcpr-stage1-independent.js");
const ROOT=path.resolve(__dirname,"../..");const SPEC_PATH=path.join(ROOT,"doc/rich-critical-position-representation/preregistration/STAGE_1_DEVELOPMENT_SPEC.json");
const DEFAULT_DIR=path.join(ROOT,"artifacts/local/rich-critical-position-representation/stage1-implementation-smoke-v1");
function ensure(c,m){if(!c)throw new Error(m);}function clone(v){return JSON.parse(JSON.stringify(v));}function sha256File(p){return crypto.createHash("sha256").update(fs.readFileSync(p)).digest("hex");}
function parseDir(){const at=process.argv.indexOf("--dir");return at>=0&&process.argv[at+1]?path.resolve(process.argv[at+1]):DEFAULT_DIR;}function technicalRandom(seed){let x=seed>>>0;return()=>{x^=x<<13;x^=x>>>17;x^=x<<5;return(x>>>0)/0x100000000;};}
function legal(state){return E.moveVariants(state).slice().sort((a,b)=>F.independentMoveKey(a).localeCompare(F.independentMoveKey(b)));}
function fixtureSearch(){const found={namua:[],mtaji:[]};for(let offset=0;offset<256&&(found.namua.length<1||found.mtaji.length<1);offset+=1){const seed=28600001+offset;const rng=technicalRandom(seed);let state=E.initialState();const history=[];for(let ply=0;ply<160&&state.winner===null;ply+=1){const moves=legal(state);const phase=state.phase;if(found[phase].length<1&&history.length>=4&&moves.length>=2){const preRootHistory=history.slice(-4).map(clone);F.independentHistoryCheck(state,preRootHistory);found[phase].push({fixtureId:`${phase.toUpperCase()}-S1-SMOKE`,sourceSeed:seed,sourcePly:ply,phase,root:clone(state),preRootHistory});}const move=moves[Math.floor(rng()*moves.length)];history.push({state:clone(state),move:clone(move)});if(history.length>4)history.shift();state=E.applyMove(state,move).state;}}
ensure(found.namua.length===1&&found.mtaji.length===1,"independent fixture search failed");return[found.namua[0],found.mtaji[0]];}
function technicalItems(fixtures){return fixtures.map((fixture)=>{const historicalTrajectoryHash=I.canonicalHash({technicalFixture:fixture.fixtureId,sourceSeed:fixture.sourceSeed,sourcePly:fixture.sourcePly});const openingPrefixHash=I.canonicalHash({technicalFixture:fixture.fixtureId,opening:false});const historyWindowHash=I.historyWindowHash(fixture.preRootHistory);const rawStateKey=F.independentRawKey(fixture.root);return{...fixture,gameIndex:-1,seed:fixture.sourceSeed,generationStratum:"TECHNICAL-FIXTURE",rawStateKey,historicalTrajectoryHash,openingPrefixHash,historyWindowHash,representationRowIdentity:I.canonicalHash({historicalTrajectoryHash,rawStateKey,ply:fixture.sourcePly,historyWindowHash})};});}
function trajectoryForFold(targetFold,spec,label,phase,ordinal){for(let n=0;n<100000;n+=1){const token=`SMOKE|${targetFold}|${label}|${phase}|${ordinal}|${n}`;if(I.foldForTrajectory(token,spec)===targetFold)return token;}throw new Error("independent fold construction failed");}
function syntheticRows(spec){const families=Object.keys(spec.developmentModel.candidateFamilySets).filter((id)=>id.endsWith("_ONLY")).map((id)=>spec.developmentModel.candidateFamilySets[id][0]);const featureNames=families.map((f)=>`${f}.smokeScalar`).sort();const rows=[];let serial=0;for(let fold=0;fold<5;fold+=1)for(const high of[false,true])for(const phase of["namua","mtaji"])for(let k=0;k<5;k+=1){const historicalTrajectoryHash=trajectoryForFold(fold,spec,high,phase,k);const numericFeatures=Object.fromEntries(featureNames.map((name,index)=>[name,(high?2:-2)+(index*0.001)+(k*0.00001)]));rows.push({serial:serial++,phase,historicalTrajectoryHash,representationRowIdentity:I.canonicalHash({historicalTrajectoryHash,serial}),primaryEstimable:true,highDivergence:high,representation:{featureNames,numericFeatures}});}return rows;}
function run(){const dir=parseDir();const prod=JSON.parse(fs.readFileSync(path.join(dir,"production-smoke.json"),"utf8"));const spec=JSON.parse(fs.readFileSync(SPEC_PATH,"utf8"));ensure(prod.specSha256===sha256File(SPEC_PATH),"smoke spec hash mismatch");const smokeSpec=clone(spec);smokeSpec.sourcePopulation.games=6;smokeSpec.sourcePopulation.seedStart=28600001;smokeSpec.sourcePopulation.seedEnd=28600006;smokeSpec.sourcePopulation.maxPly=16;smokeSpec.criticalityMeasurement.replicatesPerExactRootMove=1;smokeSpec.criticalityMeasurement.maximumPostRootContinuationPlies=12;
const games=Array.from({length:6},(_,i)=>I.runGame(smokeSpec,i));const gameSignatures=games.map((record)=>({gameSummary:record.gameSummary,candidate:record.candidate?{seed:record.candidate.seed,phase:record.candidate.phase,ply:record.candidate.ply,rawStateKey:record.candidate.rawStateKey,historicalTrajectoryHash:record.candidate.historicalTrajectoryHash,openingPrefixHash:record.candidate.openingPrefixHash,historyWindowHash:record.candidate.historyWindowHash,representationRowIdentity:record.candidate.representationRowIdentity,selectionRank:record.candidate.selectionRank}:null}));const selection=I.selectRoots(games,smokeSpec);
const items=technicalItems(fixtureSearch());const represented=I.materializeRepresentations(items,smokeSpec);const measurements=represented.map((item)=>I.measureRoot(item,smokeSpec,{replicates:1}));const fixtureSignatures=represented.map((item,index)=>({fixtureId:item.fixtureId,phase:item.phase,rawStateKey:item.rawStateKey,historyWindowHash:item.historyWindowHash,featureSchemaSha256:item.representation.featureSchemaSha256,featureVectorSha256:item.featureVectorSha256,measurementSha256:measurements[index].measurementSha256,primaryEstimable:measurements[index].primaryEstimable,dRange:measurements[index].dRange,highDivergence:measurements[index].highDivergence}));const model=I.developModel(syntheticRows(spec),spec);
const checks={gameReplay:I.canonicalHash(gameSignatures)===I.canonicalHash(prod.gameSignatures),rootReselection:selection.selectionHash===prod.selectionHash,fixtureRepresentationAndMeasurement:I.canonicalHash(fixtureSignatures)===I.canonicalHash(prod.fixtureSignatures),syntheticModel:(model.modelDevelopmentSha256||null)===(prod.syntheticModel.modelDevelopmentSha256||null)};const passed=Object.values(checks).every(Boolean);const result={schemaVersion:1,studyId:spec.studyId,stageId:spec.stageId,scientificInferenceAuthorized:false,scientificOutcomeGenerated:false,scientificStage1SeedBlockConsumed:false,checks,passed,independentSmokeSha256:null};result.independentSmokeSha256=I.canonicalHash(result);fs.writeFileSync(path.join(dir,"independent-smoke.json"),`${JSON.stringify(result,null,2)}\n`);console.log(JSON.stringify({passed,checks,independentSmokeSha256:result.independentSmokeSha256},null,2));ensure(passed,"independent implementation smoke mismatch");}
try{run();}catch(error){console.error(error.stack||error.message||String(error));process.exitCode=1;}
