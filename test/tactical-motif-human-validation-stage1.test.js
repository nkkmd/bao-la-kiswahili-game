#!/usr/bin/env node
"use strict";
const assert=require("node:assert/strict"),E=require("../public/engine.js");const Validator=require("../tools/experiments/validate-tactical-motif-human-validation-stage1-spec.js"),C=require("../tools/experiments/lib/tactical-motif-human-validation-stage1.js"),Renderer=require("../tools/experiments/lib/tactical-motif-human-validation-renderer.js");
const {spec,specSha256}=C.loadSpec();Validator.validateSpec(spec);const c03=C.loadCandidates(spec);assert.equal(c03.candidateId,"TM-S2-C03");assert.equal(c03.canonicalStage1Rank,5);assert.deepEqual(c03.preconditions,["reusablePits=0-2"]);assert.equal(c03.consequence,"actorNyumbaSeedsDeltaSign=0");
const initial=E.initialState(),north={...E.clone(initial),player:1},mirrored=Renderer.participantState(north);assert.equal(mirrored.player,0);assert.deepEqual(mirrored.pits[0],north.pits[1]);assert.deepEqual(mirrored.pits[1],north.pits[0]);const svg=Renderer.renderPositionSvg(initial);assert.match(svg,/SOUTH TO MOVE/);assert.doesNotMatch(svg,/reusable/i);assert.doesNotMatch(svg,/C03/i);assert.doesNotMatch(svg,/tesuji/i);
const technicalSpec=JSON.parse(JSON.stringify(spec));technicalSpec.population.maxPly=9;technicalSpec.population.conditionAssignment.strata=technicalSpec.population.conditionAssignment.strata.map((row)=>({...row,generator:{...row.generator,maxDepth:1,evaluationProfile:"bao",searchProfile:"phase2",quiescenceDepth:1}}));const g1=C.runGame(technicalSpec,specSha256,0),g2=C.runGame(technicalSpec,specSha256,0);assert.equal(g1.historicalTrajectoryHash,g2.historicalTrajectoryHash);assert.deepEqual(g1.moves,g2.moves);assert.equal(g1.humanDataCollectionAuthorized,false);assert.equal(C.nearC03Morphology({type:"takata",phase:"mtaji",row:1,direction:"left"}),true);assert.equal(C.nearC03Morphology({type:"takata",phase:"mtaji",row:1,direction:"right"}),false);const a={conditionId:"B-D2",ply:50,actorFeatures:{houseOwned:true,legalMoveCount:4,frontOccupied:5,boardSeeds:30}},b={conditionId:"B-D2",ply:52,actorFeatures:{houseOwned:true,legalMoveCount:5,frontOccupied:4,boardSeeds:34}};assert.ok(C.matchCost(a,b,spec)>=0);console.log(JSON.stringify({passed:true,stageId:spec.stageId,specSha256,historicalCandidate:c03.candidateId,technicalGamePlies:g1.plies,rendererActorNormalizedToSouth:true,humanDataCollectionAuthorized:false},null,2));
